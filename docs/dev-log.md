# 데일리 개발로그 — 식집사 2.0

세션마다 짧게 기록. 트러블슈팅·스펙 변경 이력도 여기 한 항목으로 같이 남긴다.
양식은 기획서 3단계 ④ 기준, 최신 항목을 맨 위에 추가한다.

> 이 문서는 **무엇을·왜** 했는지만 남긴다. 결정의 배경과 근거는 `spec.md` 결정 로그,
> 구현 세부는 코드와 커밋 메시지를 본다. 여기에 다시 옮겨 적지 않는다.

```
## YYYY-MM-DD
- 오늘 목표:
- 진행한 작업:
- 막힌 점 / 트러블슈팅:
- 스펙과 다르게 구현한 부분(있다면) + 이유:
- 내일 할 일:
```

---

## 2026-08-26

**오늘 목표** — 바이브코딩~배포 구간 문서 정합성 점검 → 5단계 3종 작성

**진행한 작업**
- **문서 4종 정합성 점검** — 키워드로 대조해 실제 구멍 2건 발견. SC-01 자동완성 대응(커밋 4개)과 🌱 브랜드 마크 통일이 어느 문서에도 없었음 → spec 결정 로그·기획서·개발로그에 보강
- 데모 모드를 기획 문서 체계에 정식 편입 — MVP 구현 범위 표 2행, QA 체크리스트 "데모 모드(12번)" 카테고리 신설
- **5단계 ① KPI 트래킹 시트** — 코드를 확인해 지표 5종을 측정 가능 여부로 분류(⭕2 · △2 · ❌1). **수치는 의도적으로 비움**
- **5단계 ② 회고 보고서** — 잘된 것 4 / 문제였던 것 4 / 다시 한다면 5
- **5단계 ③ 프로젝트 요약 원페이저** — 데모 링크와 알려진 한계를 같은 장에

**막힌 점 / 트러블슈팅**
- KPI 시트를 쓰려다 **`notifications` 테이블에 쓰는 코드가 하나도 없다는 걸 발견** — 스키마만 있고 실제 발송이 MVP 범위 밖이라 이력이 안 쌓인다. "알림 대비 완료율"이라는 원래 지표 정의를 그대로 잴 수 없어, 지표 정의를 "예정 대비"로 바꿔야 한다는 점을 시트에 명시
- 분석 패키지가 없어(`package.json` 의존성 4개뿐) 획득·수익화 지표가 통째로 측정 불가 — 숨기지 않고 "측정을 막고 있는 것"으로 정리

**스펙과 다르게 구현한 부분**
- 없음(문서 작업)

**내일 할 일**
- 미뤄둔 lint 에러 5건 정리
- 테스트 계정 정리(사용자 직접), 백업 브랜치 삭제
- (옵션) PRD 서머리 1장

---

## 2026-08-25

**오늘 목표** — 11번 ① 뒤에 남아 있던 UX 수정분 마무리 → ② 파비콘·링크 공유 미리보기 → ③ 반응형 최종 점검 → ④ 배포 마무리
> 진행 중 범위가 늘어남 — 12번 포트폴리오 데모 모드 신설, 데모 데이터로 실사용 점검(UI 결함 6건), 4단계 ① 배포 관리 작성까지 같은 날 처리

**진행한 작업**
- **"밀린 일정"을 일급 상태로 승격** — 지금까지 지난 일정은 예정일 날짜만 보여줘서 밀린 건지 알 수 없었음
  - SC-03 할일 카드에 `n일 지남`을 라임으로 표기, 90일 넘게 밀리면 `2026년 3월부터 밀림`(연·월)으로 — 기존 `dday()` 하나로 통일
  - SC-05 캘린더는 밀린 항목을 원래 날짜에만 두지 않고 **오늘 칸으로 끌어와** "밀림" 배지 + 원래 예정일 병기. 보이는 달 밖의 밀린 항목까지 별도 조회
  - 완료 직후에도 오늘 칸에서 사라지지 않게 조회 조건을 `미완료 OR 오늘 완료`로 확장
- SC-06 리스트 카드에 "분갈이 시기" 배지 — 상세에 들어가야만 알 수 있던 상태를 리스트로 끌어올림
- 캘린더 날짜 칸 점이 3개를 넘으면 `+n`으로 접기, 선택된(블랙) 칸에서 완료 점이 묻히던 것 보정
- 전 탭 sticky 제목에 스크롤 경계 그림자, SC-03 알림 배너를 라벤더 면 → 좌측 보더로(면을 덜 먹게)
- CLAUDE.md에 이번 라운드 규칙 5건 추가 + 이미 쓰고 있으면서 표에 빠져 있던 `soil` 토큰 문서화

**막힌 점 / 트러블슈팅**
- 밀린 항목을 완료하면 오늘 칸에서 사라졌음 — 완료 시 `scheduled_date`는 그대로라 원래 날짜로 돌아가 버림. 조회 조건에 "오늘 완료"를 더해 해결
- 캘린더 밀림 항목이 원래 날짜 칸에서도 "예정 2026-03-01"을 달아 제목과 중복 → 오늘 칸으로 끌어온 경우만 병기
- 남은 lint 에러 5건(`react-hooks/set-state-in-effect`)은 sc03·sc05·sc07·sc11의 기존 `useEffect` → `load()` 패턴. 이번 변경과 무관해 손대지 않음(사용자 판단으로 뒤로 미룸)
- **푸시했는데 Vercel이 `Blocked`** — 빌드 로그조차 없어 코드를 의심했지만 클린 클론 빌드까지 통과. 실제 사유는 **커밋 작성자 이메일**이었음. `git config user.email`이 로컬·전역 모두 비어 있어 git이 `사용자명@맥이름.local`을 자동 생성해 박았고, Vercel은 GitHub 계정과 매칭 안 되는 이메일의 커밋을 배포하지 않는다. 전역 설정을 `박정빈 <jungbin990218@gmail.com>`으로 복구하고 오늘 커밋 3개의 작성자를 `git rebase <base> --exec 'git commit --amend --author=...'`로 다시 쓴 뒤 force-push → 배포 통과. `--reset-author` 대신 `--author=`를 쓴 건 원래 커밋 시각을 보존하기 위해서
- 아이콘을 만들 도구가 없었음 — 이 맥에 ImageMagick·PIL이 없고 `sips`는 합성이 안 되며, 브라우저는 `file://`을 못 열고 스크린샷도 JPEG라 아이콘용으로 부적합. 결국 캔버스로 그려 임시 API 라우트로 저장하는 방식(작업 후 라우트 삭제). `app/api/_devsave`로 만들었다가 404 — App Router에서 `_` 폴더는 라우팅 제외라 `devsave`로 변경

**11번 ② 파비콘·링크 공유 미리보기**
- Next.js 기본 아이콘을 걷어내고 로고(`logo_icon.png`)에서 4종 파생 — `icon.png`(256, 투명) · `favicon.ico`(64) · `apple-icon.png`(180, 라임) · `opengraph-image.png`/`twitter-image.png`(1200×630)
- 배경을 라임/흰/ink/투명 4안으로 만들어 16·24·32px에서 나란히 비교 → **투명 풀블리드** 확정. 라임은 초록 새싹이, ink는 갈색 화분이 배경에 묻혔다
- OG 카드는 처음에 SC-01 입구 화면을 그대로 옮겼다가, 참고 이미지(좌우 2단 랜딩형)를 받아 **재설계** — 왼쪽 로고·3줄 헤드라인·기능 칩 3개 / 오른쪽 실제 앱 카드 3장. 화이트·다크 2안을 뽑아 **다크 채택**
- `layout.tsx`에 `metadataBase`·`openGraph`·`twitter` 메타 추가

**11번 ③ 반응형 최종 점검**
- iframe으로 화면 10개를 320·360·390·430px에 동시에 띄워 가로 넘침을 자동 측정 → **전 구간 넘침 0건**
- **문제 1 — 430~640px가 전체 폭으로 늘어남.** 프레임 전환 기준이 640이라 그 사이 구간(폴더블·태블릿 분할화면·좁힌 브라우저 창)에서 430 기준 화면이 그대로 늘어나 카드·케어현황 링·탭바가 벌어졌다. 기준을 **462px(430+여백 32)** 로 내려 프레임이 들어갈 수 있으면 바로 씌우게 함
- **문제 2 — 320px에서 SC-06 칩이 한 글자씩 세로로 쪼개짐.** flex가 칩을 눌러 생긴 현상. `flex-wrap` + 칩마다 `shrink-0`·`whitespace-nowrap`으로 해결
- SC-09에서 잘려 보이던 카테고리 칩·추천 목록은 `overflow-x-auto` 가로 스크롤이라 의도된 동작 — 수정 안 함

**11번 ④ 배포 마무리 — 프로덕션 전수 점검**
- 로컬이 아닌 배포본을 점검 — 라우트 13개 200 · 날씨 API 실키 응답 · 비로그인 접근 가능 · Supabase 도달 · `OPENWEATHER_API_KEY` 클라이언트 미노출 전부 확인
- **불일치 1건 발견·수정** — 화면엔 `33°`가 뜨는데 폭염 배지는 없었음. 표시는 `Math.round(temp)`(32.76→33), 판정은 `temp_max` 원값(32.78) 비교라 어긋났다. 임계값은 그대로 두고 비교만 정수로 맞춤
- 근본 한계 기록 — 무료 현재날씨 API의 `temp_max`는 지점 현재값과 사실상 같아(32.76 vs 32.78) 정책의 "일 최고기온"이 아니라 현재기온으로 판정하는 셈. 유료 예보 API 필요 → `spec.md` 확정 필요 목록에 추가
- 이로써 **③구현 착수 순서 11단계 전부 완료**

**12번 포트폴리오 데모 모드 (신설)**
- 고정 데모 계정 + 시드 + SC-01 "데모 계정으로 바로 보기" 버튼(로그인 폼 위 primary)
- 시드 — 식물 9종 / 오늘 완료 2 · 오늘 예정 2 · 지연 3 · 여유 2 + 분갈이 지연 2건, 식물마다 지난 이력 3건으로 캘린더·케어 리포트까지 채움
- 함정 2건 — (1) SC-03 진입 시 `recalcWateringSchedule`이 `last_watered_at` 기준으로 예정일을 덮어써서 `scheduled_date` 직접 지정은 첫 진입에 날아감 → `last_watered_at` 역산으로 전환 (2) `findSpecies`는 id가 아니라 한글 이름으로 찾아 첫 시드가 0건
- 데모 버튼이 primary가 되면서 기존 로그인 제출 버튼은 보조 스타일로 내림
- **시드 값이 오늘 기준 상대 날짜라 시간이 지나면 밀린다** — 보여주기 전 재실행 필요. 라우트(`/api/devseed`)는 배포본 404로 잠금
- 데모용 날씨 사전 캐싱은 **하지 않기로 결정** — 날씨를 저장하는 곳이 없어 캐싱 대상이 없고(정책은 문서에만 존재), 계정별 캐시는 데이터정의서에 없는 필드가 필요. 3안 제시 후 "실제 API 그대로" 채택(날씨 연동이 핵심 차별점이라 시연값으로 대체하면 설득력이 떨어짐)

**데모 데이터로 실사용 점검 — UI 결함 6건**
- 데이터가 채워지자 빈 화면에서는 안 보이던 문제가 드러남. 지적 4건 + 확인 중 발견 2건
- **SC-06** — "다음" 블록 라벨을 `다음`/`오늘`/`지남`으로 갈라 색과 글자가 같은 것을 말하게 함(색 규칙은 있었지만 근거가 화면에 없었음). 정렬을 등록순 → 물 줄 날짜 가까운 순으로
- **SC-07** — 보관·삭제 두 버튼을 삭제 하나로. 보관함 화면이 없는데 모달이 "다시 꺼낼 수 있어요"라고 하고 있었고, 정책정의서의 삭제 처리가 원래 한 줄이라 버튼 2개가 중복이었음. 애칭 인라인 수정 추가
- **SC-05** 밀림 배지 오른쪽 끝 정렬 · **SC-11** 프로필 세로 정렬(`min-h-11`만 주면 글자가 위쪽에 붙음)
- 지적 중 "화분 크기·배치 위치 수정 불가"는 사실과 달랐음 — 이미 각 탭에서 수정 가능하고 **발견이 안 됐던 것**이라 안내 문구로 해결

**SC-01 자동완성·브랜드 마크**
- **키체인으로 채운 이메일이 검은 글씨로 안 보이는 문제** — CSS로 자동완성 색을 이기려는 시도 3회(전환 지연 → inset 그림자 + `currentColor` → 표준 `:autofill` + `!important`) 전부 실패
- 전제를 바꿔 해결 — **입력칸을 이기려 들지 말고 흰 표면 + 검은 글자로.** 브라우저 자동완성 스타일이 전부 밝은 배경을 전제하므로 어느 색으로 덮이든 읽힌다. `paper` 토큰의 원래 정의와도 일치
- 최악의 조합(연파랑 배경 + 검은 글자 `!important`)을 강제로 입혀 검증
- SC-01 로고 → **🌱 이모지**, 이어서 파비콘 3종·OG 카드까지 같은 얼굴로 통일. iOS 홈 아이콘만 `ink` 배경(라임 위에선 초록이 묻힘)

**4단계 ① 배포 관리 작성**
- 배포 환경 표 + 푸시 전/배포 후/공개 전 3단계 체크리스트 + v1.0.0 릴리즈노트
- 항목은 일반론이 아니라 **이번에 실제로 겪은 것** 기준(커밋 이메일 차단·환경변수 누락·개발 라우트 노출·OG 캐시), 각 항목에 근거를 괄호로
- 문서로 끝내지 않고 **체크리스트를 프로덕션에 1회 실행해 전 항목 통과 확인**
- 릴리즈노트에 알려진 제약 6건 명시, `package.json` 0.1.0 → 1.0.0

**스펙과 다르게 구현한 부분**
- 밀린 항목이 캘린더에서 원래 날짜와 오늘 두 곳에 점으로 찍힘 — 원래 날짜에 일정이 있었다는 사실도 남겨야 해서 의도한 중복
- 폭염·한파 판정을 정수 반올림 기준으로 비교 — 임계값(33·-12)은 정책 그대로지만, 화면 표시와 어긋나 보이는 문제가 더 크다고 판단

**내일 할 일**
- 5단계 ① KPI 트래킹 시트 · ② 회고 보고서 · ③ 프로젝트 요약 원페이저
- 데모 데이터 재시드(보여주기 직전 — 상대 날짜라 하루만 지나도 밀림)
- 미뤄둔 lint 에러 5건, 테스트 계정 정리, 백업 브랜치 삭제

---

## 2026-08-23

**오늘 목표** — 11번 ① 실사용 UX 점검·다듬기

**진행한 작업**
- 브라우저로 SC-01~SC-12 순회 + 사용자 시나리오 1·3 재현 → **문제 18건 중 17건 반영**
  - 치명적 1건: 캘린더에서 날씨 아이콘이 오늘 날짜 숫자를 가리던 문제
  - 접근성: 보조 텍스트 대비 74곳 상향(AA 미달 → 충족), 터치 타겟 44px
  - 일관성: 탭바 아이콘을 선 아이콘 5종으로 통일, 전 탭 제목 sticky 고정
- **케어현황 링 지표 재정의** — "오늘 처리 대상 중 완료 비율"은 할 일 없는 날 늘 비어 있어서, "전체 식물 중 지금 문제없는 식물 수"로 기준 통일 + 상태 문구 추가
- **날씨 파트 강조** — 아이콘·온도 확대, 관리 팁 승격, 날씨가 일정에 반영된 결과를 항상 노출
- **오늘의 할일 완료 표현** — 완료해도 목록에 남기고 되돌리기 추가(`undoCareItem`)
- **PWA 제외 결정** → 파비콘·링크 미리보기를 11번으로 대체 편입
- OpenWeatherMap 키 발급·연결 완료, 실제 날씨로 검증

**막힌 점 / 트러블슈팅**
- 위치 권한 "허용"을 눌러도 브라우저가 차단하면 서울로 폴백되는데 이유를 안 알려줬음 → 폴백 사유를 문구에 명시. 권한 대기가 14초까지 이어지는 건 미해결
- 자동화 좌표 클릭이 스크롤·리렌더에 계속 빗나감 → 요소 참조 방식으로 전환

**스펙과 다르게 구현한 부분**
- PWA 제외 — 설치한 사람에게만 가치가 있어 포트폴리오 열람자에겐 의미 없음. `next-pwa`도 2022년 이후 방치돼 현재 버전에서 사용 불가
- SC-03 확장 요소(식물 선택 탭·다가오는 일정·날씨 팁) 유지 확정 — 기획서 SC-03에 반영
- 완료 취소 시 이전 물준 날짜는 직전 이력이 없으면 역산이라 하루 이틀 오차 가능. 정확히 하려면 데이터정의서에 없는 필드가 필요해 보류

**내일 할 일**
- 11번 ② 파비콘·링크 공유 미리보기 → ③ 반응형 점검 → ④ 배포 마무리
- QA 미검증 항목 정리(OS 권한 팝업 필요분), 테스트 계정 삭제

---

## 2026-08-21

**오늘 목표** — 구현 착수 순서 1~10번. 결과적으로 화면 12개 전부 구현

**진행한 작업**
- **1~3번 기반** — Next.js + Tailwind 셋업, Supabase 4개 테이블 + RLS, GitHub·Vercel 자동 배포 연결. 정적 데이터(식물 29종·제품 16개) 스키마 검증 및 이미지 45건 전수 확인
- **디자인 토큰화** — 참고 이미지와 5색 팔레트 기준으로 `@theme` 정의, CLAUDE.md에 디자인 섹션 신설. 데스크톱용 폰 프레임(`PhoneFrame`) 추가
- **4~6번 Phase 1** — SC-01 인증 / SC-02 온보딩 · SC-08 등록 · SC-06 리스트 / SC-03 오늘 · SC-04 빈 상태 · SC-07 상세 + 하단 탭바. 물주기 계산은 데이터정의서 공식대로 구현하고 문서의 검증 예시 6건과 대조해 일치 확인
- **7~10번** — SC-07 분갈이·배치 섹션, SC-05 캘린더, SC-11 마이·SC-12 계정, SC-09·SC-10 제품 + 케어 리포트
- **날씨** — 키를 서버에만 두는 API 라우트 경유, 3초 타임아웃·폴백 처리. 실제 날씨로 만들 수 없는 폭염·한파·장마를 시연·검증할 수 있게 시나리오 스위치(`?weather=장마`) 추가

**막힌 점 / 트러블슈팅**
- 배포 URL이 백지라 빌드 실패로 오판 → **타인이 선점한 동명 도메인**이었음. 이 프로젝트는 `-jungvin` 접미사 쪽
- Vercel Deployment Protection이 신규 프로젝트 기본 on이라 로그인해야만 접속 가능 → 해제
- `Special:FilePath` 이미지 링크가 1장당 리다이렉트 2번을 타서 45장이 135요청이 되고 레이트리밋에 걸림 → CDN 직접 링크로 전량 교체
- `next dev`가 실행마다 `CLAUDE.md`에 자체 블록을 덧붙임 → `agentRules: false`
- 인증 에러를 메시지 문자열로 매칭했다가 마침표 차이로 실패 → `error_code` 기준으로 변경
- 폰 프레임이 창 높이에 따라 비율이 뭉개짐 → 내부를 430×860으로 고정하고 통째로 축소

**스펙과 다르게 구현한 부분**
- `products`·날씨 테이블 미생성 — 제품은 JSON 단일 소스, 날씨는 조회성. 실제 테이블 4개
- `profiles`에 `login_account` 없음(`auth.users.email`이 보유), 위치는 좌표/지역명을 한 필드에 저장
- 회원가입 트리거(`handle_new_user`) 추가 — `auth.users` ↔ `profiles` 1:1 보장용
- 문서에 정의가 없어 임의로 정한 것 — SC-01 에러 문구, 장마 판정의 시간당 강수량 환산, 날씨 예외 우선순위, 캘린더 배지 색 기준, 케어 리포트 지표. **전부 `spec.md` 확정 필요 목록에 있음**
- 범위 밖으로 남긴 것 — 회원탈퇴 시 계정 삭제(service_role 필요), 식물별 알림(데이터정의서에 없는 필드), 캘린더 과거·미래 날씨(유료 API 필요)
