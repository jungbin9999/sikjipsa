# 식집사 2.0 — 프로젝트 진행 스펙 (spec.md)

> 이 파일은 프로젝트 전체 진행 상황을 추적하는 살아있는 문서입니다. 각 단계가 진행될 때마다 상태와 결정사항을 갱신합니다.
> 결정 로그는 각 단계 섹션 아래에 있습니다. 같은 날짜에 여러 건이 쌓이면 토글로 묶여 있으니, 필요할 때 펼쳐서 보세요.
> 마지막 업데이트: 2026-08-25 (11번 ① UX 점검 2차 마무리, ② 파비콘·링크 공유 미리보기)

## 프로젝트 개요

- 서비스명: 식집사 2.0 — 날씨를 아는 AI 식물집사
- 목적: 서비스기획/주니어 PM 직무 포트폴리오 (개발자 X, 기획자 관점 문서 체계 구축)
- 기존 자산: 2025년 v1.0 기획(날씨 연동 케어 캘린더) → 2026 AI 결합 고도화

## 프로젝트 전반 결정 로그

문서 체계·로드맵 구성 등 특정 단계에 묶이지 않는 결정들입니다.

<details>
<summary>2026-08-18 (7건)</summary>

- 앱 프로토타입(PWA)/배포는 3~4단계에서 진행하기로 하고, 지금은 기획 문서 체계부터 단계적으로 완성하기로 결정 (개발 코드 작업 일시 중단)
- 리서치 3분할(배경/트렌드/시장) → 1개 통합 문서로 슬림화
- 요구사항정의서 삭제 (2단계 상세기획 산출물이 역할 겸함)
- 데이터 정의서 신규 추가 (2단계)
- 정책정의서에 예외처리 규칙 섹션 추가 결정
- 화면설계서에 화면 흐름도(스크린맵) 포함하기로 결정
- spec.md(진행상태 추적)와 식집사 업그레이드 프로젝트.md(실제 콘텐츠 누적) 파일 역할 분리

</details>

<details>
<summary>2026-08-19 (1건)</summary>

- **제목-소제목 정리.** 여러 개념이 한 제목에 묶여 있던 항목을 단일 개념 제목 + 내부 소제목 구조로 전면 정리 — ② STP·페르소나·여정지도 → "타겟 정의 — 누구를 위한 서비스인가" (내부: STP/페르소나/여정지도), ③ 서비스 설계(IA+기능정의·우선순위) → "서비스 설계" (내부: IA/기능정의·우선순위), ⑥ KPI/OKR 정의 → "성공지표 정의" (내부: KPI/OKR), 2단계 ①→"서비스 플로우"(내부: 사용자시나리오/플로우차트), ②→"화면설계서"(내부: 화면흐름도/화면별상세정의), ④→"정책정의서"(내부: 정책/예외처리규칙), 3단계 ①→"QA 체크리스트"(내부: 체크리스트/인수기준), 4단계 ①→"배포 관리"(내부: 체크리스트/릴리즈노트). spec.md 로드맵 표도 동일하게 갱신

</details>

<details>
<summary>2026-08-20 (1건)</summary>

- **전체 문서 말투 정리 — 명사형 불렛 전환.** 사용자가 반복 지적("명사형으로 불렛으로 나눠서 해라고 몇 번 말해 제발 지켜")한 서술형 "~했다/~한다/~이다" 문장을 문서 전체에서 명사형 종결 불렛으로 전면 교체 — 신규 작성분(2단계 ①서비스 플로우/②화면설계서 인트로·블록쿼트)뿐 아니라 이전에 손대지 않고 남아있던 부분(0단계 핵심결론/결론 블록쿼트, 1단계 페르소나 "문제:" 필드, ④비즈니스모델·⑤MVP로드맵·⑥성공지표 인트로 문장)까지 전수 검토·수정. 페르소나 "니즈:" 인용문(직접화법)은 명사형 전환 대상에서 제외(예외로 유지). 수정 후 3회 Grep 검증(서술형 종결 패턴)으로 문서 본문에 잔여 없음 확인 — 헤더의 합니다체 안내문·섹션 제목은 별개 성격으로 판단해 대상에서 제외

</details>

## 전체 로드맵 및 산출물 상태

범례: ☐ 미착수 · 🔄 진행중 · ✅ 완료

### 0단계. 리서치

| 산출물 | 상태 | 비고 |
|---|---|---|
| 리서치 보고서 (배경·트렌드·시장분석·사용자리서치·경쟁사벤치마킹·종합인사이트, 1개 문서 통합) | ✅ | 확정. AI는 "예측 보조 도구"로 포지셔닝 확정 |

**결정 로그**

<details>
<summary>2026-08-18 (8건)</summary>

- 0단계 리서치 초안 작성 완료 — 2025 1인가구 통계(36%, 절반 외로움), 반려식물 인지도 87.9%, TAM/SAM/SOM 추정 로직, 2026 신규 AI 경쟁앱 반영. 단, 시장규모 수치(2조원 vs 1조7천억) 출처 기준 상이 이슈 확인 → 각주 처리 예정
- 출처 검증(팩트체크) 진행 — WebFetch로 본문 대조. 오류 3건 정정(외로움 48.9%로 정확화, 인지도 87.9%는 2022년 조사로 재라벨링, 시장규모 2조4,215억/34%로 정확화), 근거 없는 문장 1건 삭제("챗봇 UX 선호"), v1.0 시장성장 차트는 검증치와 상충해 최종본 보류 결정. "20대 65%"는 robots.txt로 본문 대조 실패, 제목 수준 확인으로 표시
- 표 남발 지적받아 0단계 전체를 서술형으로 재작성 (표는 v1.0↔2026 비교, 경쟁사 비교 2곳만 유지)
- 논리적 공백 점검 — "20대 65%"를 검증된 "30대 이하 37.2%"(농촌진흥청 2024.9, v1.0의 37.1%와 사실상 일치)로 교체. "1인가구·외로움→반려식물" 연결고리를 데이터솜/농촌진흥청 자료(정서교감 목적 54.8%, 1인가구 생명존중 공감도 73%)로 보강. "1인가구=젊은층"이라는 착각을 1인가구 연령구성 데이터(70세 이상 최다)로 바로잡고, 타겟팅 근거를 반려식물 양육률(30대 이하 37.2%)로 재구성. 초보자 실패율 관련 블로그 2건은 원문 대조 결과 출처 없음 확인, 폐기
- 여전히 못 채운 공백 확인 — ①디지털 앱 수요 데이터(TAM은 있으나 SAM 근거 약함) ②초보자 실패율 정량 데이터 ③"물주기 타이밍 망각"의 2026 재검증. 세 가지 모두 문서에 명시적으로 남겨둠
- 문서가 지나치게 길고 경고문(⚠️)이 많다는 피드백 반영 — 0단계를 결론 중심으로 재편집, 검증 이력은 spec.md로 이관, 참고자료는 간결한 서지 목록으로 정리
- **핵심 전략 방향 결정 — AI 포지셔닝 재정의.** 당초 "AI 진단·상담"을 차별화 요소로 넣었으나, 사용자 지적으로 재검토: (1) 0단계 리서치의 페인포인트 3가지는 모두 예방/예측 관련이고 진단 수요는 발견된 바 없음 (2) 경쟁사(Leafora/Botai/그루우)가 이미 AI 진단을 정체성으로 갖고 있어 따라하기가 됨 (3) 식집사의 본질은 "날씨×식물특성 기반 예측형 케어"임. → AI의 역할을 "사후 진단"에서 "예측 정확도를 높이는 보조 도구"(예: 등록 시 사진으로 식물 종류 자동 인식)로 재정의. 0단계 결론/트렌드 비교 문구 전면 수정
- **MVP 범위 결정 — 비용·복잡도 제약 반영.** AI 식물 인식(PlantNet 등)은 무료 여부 확답 불가, AI 환경(광량) 추정은 정확도 낮고 구현 난이도 높음 → 둘 다 기획문서(향후 확장 로드맵)에는 포함하되 실제 바이브코딩 MVP 구현에서는 제외 결정. MVP 핵심 기능은 **직접 검색/선택 방식의 식물 등록(기본 중의 기본) + 날씨 연동 캘린더 + 알림/리마인더 + 케어 이력**로 확정

</details>

<details>
<summary>2026-08-19 (2건)</summary>

- **문단 양식 통일 — 1단계 스타일에 맞춤.** 1단계(①~④)는 볼드 라벨+짧은 불릿 중심으로 잘 읽히는데 0단계는 줄글(narrative prose) 위주라 이질감이 있다는 피드백 반영. 내용·통계·출처는 그대로 두고, "왜 지금인가"/"무엇이 달라졌나"/"시장은 충분히 크다"/"사용자는 여전히 같은 문제를 겪는다" 등 각 섹션을 볼드 라벨 불릿 리스트 구조로 재작성. 핵심결론/결론은 인용구(blockquote) 형태로 통일, 다음 리서치 과제·MVP 범위 메모도 라벨+불릿으로 정리
- **전문용어 제거 — TAM/SAM/SOM.** 무슨 뜻인지 모르겠다는 피드백으로 "시장은 충분히 크다" 섹션의 TAM/SAM/SOM 라벨을 "전체 시장 / 우리가 노릴 시장 / 출시 초기 목표"로 교체 (내용은 동일, 용어만 쉬운 말로). spec.md 내부 로그에는 원래 용어(TAM/SAM 등) 그대로 남겨둠 — 이 파일은 공유용이 아니라 작업 기록용

</details>

### 1단계. 상위기획 (Product Strategy)

| 산출물 | 상태 | 비고 |
|---|---|---|
| ① 서비스 전략 | ✅ | [확정] |
| ② 타겟 정의 — 누구를 위한 서비스인가 (STP / 페르소나 / 여정지도) | ✅ | [확정] |
| ③ 서비스 설계 (IA / 기능정의·우선순위) | ✅ | [확정] |
| ④ 비즈니스모델 | ✅ | [확정] |
| ⑤ MVP 로드맵 | ✅ | [확정] |
| ⑥ 성공지표 정의 (KPI) | ✅ | [확정] |

**결정 로그**

<details>
<summary>2026-08-19 (12건)</summary>

- 1단계 ① 서비스 전략 섹션을 참고 이미지(라벨/명사형 스타일) 톤에 맞춰 재작성 (한줄컨셉/비전·미션/서비스의필요성/핵심기능3가지/차별화포인트/기대효과 구조). "v1.0에서 출발" 반복 언급 제거, 신규·업그레이드 파트에만 (신규) 표기
- 1단계 ② 타겟 정의 초안 작성. STP는 0단계 검증 데이터(30대 이하 37.2% 양육률, 1인가구 36.1%, 정서교감목적 54.8%)로 재구성. 페르소나 3종은 v1.0 원본(지민/수현/영빈) 유지하되, AI 진단·식별 니즈는 전략 재포지셔닝에 맞춰 전부 제거하고 화분갈이·화분위치(신규 기능) 관련 니즈로 대체. 여정지도도 동일하게 "AI 진단/챗봇" 단계를 "화분갈이·위치 알림 대응" 단계로 교체 (표는 여정지도 1곳만 유지, 나머지는 라벨형 리스트로 서술)
- **Positioning 정정 — 출발점 재확인.** "사전예측 vs 사후진단" 구도만 내세우면 식집사의 원래 출발점(초보 식집사)이 흐려진다는 지적 반영. 식물마다 물주기·햇빛(양지/반양지)·습도 기준이 다르다는 것 자체를 초보자는 모른다는 게 진짜 출발점이고, 날씨 연동 예측은 그 위에 얹히는 정교화 단계라는 순서로 Segmentation/Positioning 문구 수정
- **핵심 기능 우선순위 재확인 — 물주기가 1순위.** 물주기·화분갈이·화분위치 3가지 기능이 동등하게 보이던 것을 정정. 사용자 관찰(주변 지인 사례) 근거: 초보 식집사가 식물을 죽이는 가장 흔한 원인은 과습(물을 너무 자주 줘서 뿌리썩음)이며, 식물과 날씨(온도·습도)에 맞게 물주기 타이밍만 지켜도 대부분의 식물은 산다. 화분갈이·화분위치는 같은 맥락의 문제이지만 우선순위는 물주기보다 낮음. ①서비스전략(한줄컨셉/필요성/핵심기능/기대효과), ②STP Positioning 문구를 "물주기 예측이 핵심, 화분갈이·화분위치는 확장"이라는 위계로 전면 수정. ※ 과습 원인 관찰은 사용자의 직접 경험/주변 사례이며 별도 통계 출처는 없음 — 문서에는 정량 데이터가 아닌 제품 인사이트로 표기
- 1단계 ③ 서비스 설계 초안 작성. IA는 v1.0의 5탭(홈/내식물/케어캘린더/AI상담/마이페이지)에서 AI상담 탭을 제거해 4탭으로 재구성 (AI는 별도 탭이 아니라 식물등록 보조기능으로만 존재). MoSCoW 재정리 — Must를 ①(물주기·날씨연동·캘린더·알림·등록·이력)과 ②(화분갈이·화분위치)로 순서를 나눠, "물주기 최우선 구현" 원칙을 기능 우선순위표에도 반영. **MVP 범위 정정**: 08-18에 정한 "MVP=물주기+알림+등록+이력" 좁은 정의를 "화분갈이·화분위치도 Must에 포함, 단 물주기 다음 순서로 구현"으로 갱신 — 사용자 확인 필요 (다음 액션에 추가)
- **IA 재설계 — v1.0 프로토타입 스크린샷 반영.** 사용자가 공유한 v1.0 프로토타입 화면(캘린더/오늘의 케어/식물등록/내식물/푸시) 분석해 IA를 5탭(오늘/달력/내식물/제품/마이)으로 재구성. "제품" 탭은 애초에 삭제 대상(Won't)이었으나, 사용자 확인 결과 "제품 마케팅 수수료" 비즈니스모델과 직결된 탭임이 밝혀져 MoSCoW를 Should로 정정하고 Won't에서 제외. 케어현황 링은 v1.0의 물/영양제/분갈이/상태 4종에서 물주기/분갈이/화분위치 3종으로 좁혀 확정(영양제·상태는 범위 밖, 사용자 확인). 식물 등록 폼에 "화분 크기"·"현재 배치(수동 선택)" 2개 필드 추가 제안
- 1단계 ④ 비즈니스모델 초안 작성. v1.0 수익모델 다이어그램(고객/B2B파트너쉽/리소스/수익모델 선순환 구조) 검토 — 구조는 유지하되 (1) 고객을 ②에서 확정한 "30대 이하 자취 초보 식집사"로 구체화 (2) 리소스에 화분갈이·화분위치 판단용 데이터 추가 (3) B2B파트너쉽 중 원예전문가 콘텐츠 협력은 초기 BD 난이도가 높아 확장단계로 재분류, 홈가드닝 브랜드·쇼핑몰 제휴는 제품탭과 직결되어 1순위 유지 (4) 수익모델 3종(구독/제휴/광고)을 동시 나열하던 것을 단계별 우선순위(1순위 제휴수수료 → 2순위 프리미엄구독 → 3순위 광고)로 재정리. **광고수익 처리**: 0단계 리서치(반려식물 목적 1위=정서적교감 54.8%)와 상충 우려 제기 → 사용자 확인 결과 완전 배제 대신 "네이티브형으로 제한적 포함"(배너형 배제, 제품추천과 결합된 형태만) 결정
- **1단계 ①~④ 전체 검토(10년차 PM 관점).** 사용자 요청으로 지금까지 작성한 1단계 전체를 재점검, 발견한 문제 수정: (1) "과습이 초보자 실패 1위 원인"이라는 핵심 주장에 출처가 없어 WebSearch/WebFetch로 검증 — 정확한 통계는 못 찾았으나 UConn Extension(대학 원예 교육기관)의 "물주기 불규칙이 화초 고사 1위 원인"이라는 자료를 찾아 인용 추가, 과습은 그 하위 사례로 톤 조정 (2) ②Targeting에서 "30대 이하"와 "1인가구"를 "실제로 겹치는 집단"이라 단정한 문장 발견 — 이는 0단계에서 이미 한 번 정정했던 것과 동일한 논리적 비약(1인가구는 고령층 비중도 높음)이 STP 재작성 과정에서 재발한 것 → 통계적으로 동일 집단이라 단정하지 않는다는 단서를 추가해 정정 (3) ①의 "화분 위치 가이드" 설명이 마치 광량을 자동 감지하는 것처럼 읽혀 ③에서 정한 "수동 입력 기반"과 어긋남 → 문구 명확화 (4) ②여정지도에 ③④에서 핵심 수익채널로 정한 "제품" 탭 접점이 전혀 반영 안 되어 있어 "화분갈이·위치 알림 대응" 단계에 제품 추천 노출 연결 추가 (5) ③MoSCoW의 Must/Should 기준과 ④수익모델의 1·2·3순위 기준이 서로 다른 축인데 구분 설명이 없어 혼동 우려 → 명시적 설명 추가 (6) ④리소스 목록에 "제품" 탭 운영에 필요한 제품·화분 카탈로그 데이터가 빠져 있어 추가 (7) 화분갈이·화분위치가 핵심기능으로 격상됐는데 이를 뒷받침하는 사용자 리서치 근거가 없다는 점을 확인 — 0단계 "다음 리서치 과제"에 명시적으로 추가해 추적. 큰 틀(전략 방향·우선순위)은 변경 없음, 문서 내적 정합성·근거 보강 위주 수정
- **줄글 압축 — 라벨+불릿 스타일 재적용.** "서비스의 필요성"/②Targeting/②Positioning/③MoSCoW 설명 등에 검토·보완 과정에서 다시 길어진 줄글 단락이 왜 필요하냐는 지적. 내용(수치·근거·인과관계)은 그대로 두고 문장 연결어("첫째/둘째/다만/두 근거를 합치면" 등)만 제거해 라벨+짧은 불릿 구조로 압축. ④ 비즈니스모델의 유사한 줄글 단락(수익원 동시 미가동 이유)도 동일하게 압축. 문서 상단 안내문("본문은 서술형으로 쓰고...")도 실제 스타일(라벨+불릿 우선, 줄글은 예외적으로만)에 맞게 수정
- 1단계 ⑤ MVP 로드맵, ⑥ 성공지표 정의 초안 작성. ⑤는 ③MoSCoW·④수익모델 우선순위를 4개 Phase로 시간순 배열 — Must였던 화분갈이·화분위치를 Phase 2로 분리해 "MVP 범위 재정의 최종 확인 필요" 이슈를 해소(물주기만으로 Phase 1 완결, 분갈이·위치는 곧바로 이어 붙이는 구조). ⑥은 KPI를 획득/활성화/핵심가치검증/리텐션/수익화 5단계로 정의하고 첫 분기 OKR 3개 작성 — 목표 수치(%)는 실사용 데이터가 없어 임의로 채우지 않고 의도적으로 비워둠(첫 분기 목표를 "측정 체계 확보"로 설정)
- **OKR 제외 결정.** 포트폴리오 프로젝트인데 OKR이 꼭 필요한지 의문 제기 — OKR은 원래 여러 팀이 분기 단위로 목표를 정렬하는 조직 차원의 goal-setting 도구인데, 실행 팀도 실사용자도 없는 1인 포트폴리오 프로젝트에 붙이면 형식만 갖춘 것처럼 보일 위험이 있음. 특히 목표 수치를 비워둔 상태라 "측정 체계 확보가 목표"라는 문구가 다소 군더더기로 느껴짐 → OKR 섹션 전체 삭제, KPI만 유지. 대신 (1) ⑤ MVP 로드맵에 "Phase 전환 기준"을 추가해 어떤 KPI가 확인되면 다음 Phase로 넘어가는지 명시 (2) ⑥ KPI 표의 각 지표에 "Phase 1부터/Phase 2 완성 후부터/Phase 3부터"처럼 로드맵과의 연결점을 표시 — OKR이 하려던 역할(지표와 실행계획의 연결)을 이 두 가지로 대체. 문서 헤딩에서 "(KPI/OKR)" 표기도 "(KPI)"로 정정
- **1단계 [확정].** 사용자가 추가 수정 없이 2단계로 진행 요청 — ①~⑥ 상태를 ✅ [확정]으로 변경, 콘텐츠 문서에도 "## 1단계. 상위기획 [확정]" 표기

</details>

### 2단계. 상세기획 [확정] (Detail Planning)

| 산출물 | 상태 | 비고 |
|---|---|---|
| ① 서비스 플로우 (상세 IA / 사용자 시나리오 / 플로우차트) | ✅ | 플로우차트 핵심 기능별 6개 + 예외 분기 목록, 정합성 검토 반영 완료 |
| ② 화면설계서 (화면 흐름도 / 화면별 상세정의) | ✅ | 화면 흐름도(12개 화면) + 핵심 화면 6개 상세정의 + 나머지 6개 축약표, 정합성 검토 반영 완료 |
| ③ 데이터 정의서 (엔티티·속성 정의, 기획자 레벨) | ✅ | 데이터 객체 6개 + 속성표(영문 필드명 포함) + 관계도 + 핵심 계산 로직(물주기·분갈이 공식), 정합성 검토 반영 완료 |
| ④ 정책정의서 (정책 / 예외처리 규칙) | ✅ | 정책 12건 + 예외처리 규칙 7건 + 예외 결합 규칙 3건 표, 정합성 검토 반영 완료 |

**결정 로그**

<details>
<summary>2026-08-19 (5건)</summary>

- 2단계 ① 서비스 플로우 초안 작성. 사용자 시나리오는 ②페르소나(지민/수현/영빈) 그대로 활용해 각자의 핵심 니즈에 대응하는 시나리오 1개씩(물주기/분갈이/날씨대응) 작성 — 새 인물 만들지 않고 기존 페르소나와 1:1 매칭. 플로우차트는 실제 다이어그램 대신 화살표 텍스트 체인으로 표현(메인 플로우 1개 + 예외 분기 2개: 날씨 변수, 알림 미실행) — Notion 붙여넣기에는 이 형태가 더 적합하다고 판단
- **플로우차트 Mermaid 전환 + 순서/스코프 정리.** (1) 화살표 텍스트 대신 실제 Mermaid flowchart 코드블록 3개(메인/날씨분기/미실행분기)로 전환 — Notion에 붙여넣으면 최소 코드블록으로는 보이고, Mermaid 블록에 직접 붙여넣으면 다이어그램으로도 렌더링 가능 (2) IA는 1단계 ③에 있으니 2단계에서 중복 작성하지 않는다는 점을 섹션 상단에 명시 (3) "사용자 시나리오부터 시작하는 게 맞냐"는 질문에 — 통상 전체 그림(플로우차트)을 먼저 보여주고 시나리오로 구체화하는 순서가 더 표준적이라 판단해 순서를 플로우차트 → 사용자 시나리오로 변경 (4) ②여정지도(1단계, 감정 중심)와 서비스 플로우의 사용자 시나리오(행동·시스템반응 중심)가 목적이 다르다는 점도 섹션 상단에 명시해 중복처럼 보이지 않게 함
- **상세 IA 추가 — "1단계에 있으니 생략" 판단 정정.** 사용자가 체크앤고 서비스의 IA 참고 이미지(1단계 카테고리·2단계 화면·3단계 상세기능, 3단 구조) 공유하며 지적: 1단계 IA는 "5개 탭이 있다" 수준의 상위 구조일 뿐이고, 상세기획에서는 그걸 재사용하지 말고 화면·상세기능까지 펼쳐서 "상세하게" 다시 짜야 한다 — 위 결정(2)에서 "1단계 IA 재사용, 2단계에서 생략"이라고 판단한 것이 잘못이었음. ①서비스 플로우에 "상세 IA" 소제목 추가, 카테고리(5개 탭)→화면→상세기능 3단계 표 + 참고 이미지와 유사한 구조의 Mermaid 다이어그램(하단 탭바에서 5개 탭 서브그래프로 분기, 각 서브그래프 안에 화면과 상세기능 노드) 작성. 표+다이어그램 둘 다 제공 — 표는 Notion에서 항상 정상 렌더링되고, 다이어그램은 Mermaid 블록에 붙여넣을 때를 위한 보조
- **상세 IA·플로우차트 누락 기능 점검 및 보완.** 사용자 요청으로 기존 상세 IA 표·메인 플로우를 다시 검토해 빠진 항목 추가. IA 표: 위치 권한 거부 시 서울 기준 기본값(신규 — 실제 `lib/weather.js`의 서울시청 좌표 기본값 로직과 일치시킴), 알림 권한 허용/거부(신규), 오늘 탭 "빈 상태" 화면(신규 — 식물 미등록 상태), 완료 체크 액션(오늘·달력 양쪽에 명시), 물주기 이력 로그·식물 삭제/보관(내 식물 상세), 월간·주간 뷰 전환 토글(달력 — 1단계 ①에 이미 언급됐으나 상세 IA엔 누락돼 있었음), 제품 상세·카테고리 필터(제품), 계정 관리(로그아웃/회원탈퇴, 마이) 총 9개 항목 추가. Mermaid IA 다이어그램에도 동일 항목 반영. 플로우차트: 메인 플로우에 "온보딩 건너뛰기" 분기(`ONB`)를 추가해 상세 IA 표와의 불일치 해소, 예외 분기 2개 신규 추가(위치 권한 거부 / 알림 권한 거부) — 알림 권한은 서비스의 핵심 가치(물주기 예측 알림)가 의존하는 전제라 거부 시 대응이 특히 중요하다고 판단
- **상세 IA를 체크앤고 참고 이미지 스타일 PNG로 교체.** 사용자가 참고로 공유한 "체크앤고 — 정보구조도" 이미지(카테고리/화면/상세기능 3단, 연회색·흰색·진회색 박스 + 트렁크형 커넥터 + 우측 상단 범례)와 동일한 형태로 만들어달라고 요청 — 기존 Mermaid flowchart LR로는 이 grid 정렬·엘보 커넥터 스타일을 낼 수 없어서, HTML/CSS(grid 레이아웃 + JS로 후처리 SVG 커넥터 드로잉)로 직접 제작한 뒤 Puppeteer(사전 설치된 Chromium, mermaid-cli 번들 사용)로 PNG 렌더링(`ia_diagram2.png`). 상세 IA 표의 여러 상세기능 항목은 참고 이미지처럼 화면 1개당 1개의 진회색 바에 "·"로 묶어 표현. 문서의 기존 Mermaid IA 다이어그램은 이 PNG로 대체 — 문서에는 첨부 이미지 안내 문구만 남기고, PNG 파일은 노션에 직접 드래그해서 삽입하는 방식으로 안내
- **플로우차트 코드 전달 방식 — 텍스트 파일로 전환.** 채팅 안의 ```mermaid 코드블록이 (노션과 마찬가지로) 다이어그램으로 렌더링되어 사용자가 원문 텍스트를 못 보는 문제 발생. 순수 .txt 파일로 코드를 전달하면 렌더링되지 않고 원문 그대로 보인다는 점을 활용해 전환. 1차로 5개 블록을 하나의 txt 파일에 모아 전달했으나, 사용자가 파일 안의 라벨 줄("[1] 메인 플로우")과 구분선("====")까지 통째로 노션 Mermaid 블록에 붙여넣어 파싱 오류 발생 — 이후 파일당 코드 1개만 담기도록 5개 파일로 완전히 분리해 재전달. **교훈: 노션 Mermaid 블록에는 `flowchart TD`로 시작하는 코드만 들어가야 하며, 라벨/구분선이 섞이면 파싱 실패 — 코드 전달용 파일은 항상 순수 코드만 담을 것.**

</details>

<details>
<summary>2026-08-20 (14건)</summary>

- **상세 IA 재제작 — 그룹핑 + 교차연결 구조로 업그레이드.** 사용자가 두 번째 참고 이미지("체크앤고" 프로젝트관리 도구 IA — 로그인/비로그인 영역을 색깔 테두리로 그룹핑, 화면이 하위화면까지 다단으로 뻗는 가변 depth, 서로 다른 카테고리 간 실제 연결을 보여주는 긴 트렁크 라인 포함)를 공유하며 "기존 diagram png는 참고하지 말고, 구조적으로 더 영리하게, 연결이 어떻게 이어지는지 보이게" 재작성 요청. `ia_diagram2.html`을 기반으로 새 HTML(`ia_diagram2.html` 덮어씀)을 제작: (1) 카테고리/화면 아래에 가변 depth(하위화면 단계 추가 — 예: 식물 상세 → 물주기 기준·이력/분갈이 이력/배치 위치/삭제·보관, 마이 → 설정 → 프로필 관리/위치 설정/알림 설정) 도입 (2) 전체를 "로그인 후 메인 영역"(파란 테두리)과 "예외·대체 흐름 영역"(주황 테두리, 온보딩·권한 거부·빈 상태를 별도로 묶음)으로 그룹핑 (3) 실제 존재하는 관계 6개를 점선 교차연결로 표시 — 건너뛰기→빈 상태, 빈 상태→식물 등록, 위치 권한 거부→마이의 위치 설정, 알림 권한 거부→마이의 알림 설정, 분갈이 알림 연동 추천→분갈이 이력, 완료 체크 액션→물주기 이력 (모두 이미 문서에 있던 내용을 연결한 것이고 새로 지어낸 기능은 없음). 렌더링 과정에서 버그 2개 발견·수정: ① `#grid`가 `width:auto` 블록 요소라 부모(`#wrap`) 너비에 맞춰 압축되어 실제 컬럼 합계보다 좁게 측정되는 바람에 교차연결 lane과 그룹박스 우측 경계가 실제 콘텐츠보다 안쪽에 그려져 다크 바 텍스트를 가로지르는 버그 → `#grid`에 `width:max-content` 지정으로 해결 ② SVG 오버레이가 `width:100%`(부모 기준)로 커넥터·그룹박스를 그려서 그리드 우측 여백(lane 공간)이 SVG 캔버스 밖으로 잘리는 문제 → SVG에 명시적 px 크기 지정 + `overflow:visible`로 해결. 최종 파일명 `ia_diagram_v2.png`로 문서에 반영, 기존 `ia_diagram2.png` 참조를 교체
- **플로우차트 재구성 — 핵심 기능별 다이어그램 + 예외 분기 목록화.** 사용자 지적: 기존 플로우차트는 "메인 플로우" 하나에 여러 기능(등록·알림·분갈이·리텐션)을 억지로 한 줄로 이어붙인 형태였고, 앱 진입, 식물 등록 후 상세 액션, 캘린더 탭 이용, 마이페이지 이용처럼 실제 핵심 기능별 흐름이 따로 없었음. 대응: 기존 "메인 플로우" 1개를 폐기하고 핵심 기능 흐름 6개로 재구성 — ①앱 진입→홈 화면 ②물주기 알림 대응(핵심 가치 루프) ③식물 등록→상세 액션 ④캘린더(달력) 탭 이용 ⑤마이페이지 이용 ⑥제품 탭 이용. 반대로 예외 분기(위치·알림 권한 거부, 날씨 변수, 알림 미실행, 온보딩 건너뛰기)는 분기가 단순해 다이어그램 대신 글머리 기호 목록(명사형 종결)으로 압축 — 다이어그램 개수를 늘리는 대신 핵심/예외의 표현 밀도를 다르게 가져가는 방식으로 정리. 6개 다이어그램 코드는 채팅 내 mermaid 코드블록이 렌더링되어 원문을 못 보는 문제(직전 결정 참고)를 피하기 위해 처음부터 순수 .txt 파일 6개(`mermaid_blocks_v2/`)로만 전달, 코드블록으로 채팅에 노출하지 않음
- **② 화면설계서 초안 작성.** 작성 전 "화면 흐름도/화면별 상세정의에 뭐가 필수인지 먼저 정의해달라"는 요청에 따라 두 소제목의 정의(화면 흐름도=UI 내비게이션 지도, 화면별 상세정의=화면 단위 스펙)와 상세정의 필수 8항목(화면ID·진입경로·목적·구성요소·데이터·액션과결과·상태분기·정책참조)을 먼저 제시하고, AskUserQuestion으로 "핵심 화면만 상세히 vs 전체 동일 밀도" 스코프를 확인(→ 핵심 화면만 선택). 이후 사용자가 "①서비스 플로우와 뭐가 다르냐"고 재질문 — 노드 구성(서비스플로우는 화면+시스템처리+로직 혼합 vs 화면흐름도는 화면만), 스코프(핵심 기능 6개 부분 vs 전체 화면 12개 전수), 엣지 라벨(조건·로직 vs UI 액션·전환방식)의 3가지 차이로 설명해 납득시킨 뒤 작성 진행. 화면 흐름도는 상세 IA의 화면 12개를 SC-01~SC-12로 ID화해 Mermaid로 작성(전환방식 push/pop/replace/tab전환 표기, 하단 탭바 back 스택 정책 등 공통 내비게이션 규칙 3개 추가). 화면별 상세정의는 핵심 6개(오늘의 케어요약/월간캘린더/식물상세/식물등록/제품추천리스트/마이페이지홈)를 8항목 풀스펙으로, 나머지 6개(로그인/온보딩/빈상태/식물리스트/제품상세/계정관리)는 진입경로·목적·핵심액션만 담은 축약 표로 작성
- **② 화면설계서 검토·보완.** 사용자 요청으로 상세 IA·6개 핵심 기능 플로우차트·MoSCoW/로드맵과 교차 검토, 빠진 부분 2건·불필요한 내용 1건 발견해 수정. (1) 푸시 알림 탭 진입 경로 누락 — 핵심 기능 흐름②(물주기 알림 대응)의 외부 진입점이 화면 흐름도에 반영 안 돼 있던 문제, "푸시 알림 탭" 노드를 추가하고 SC-03(오늘의 케어 요약)으로 딥링크 진입(replace)하는 엣지로 정의, 공통 내비게이션 규칙에도 4번째 항목으로 명문화 (2) SC-07 삭제·보관 확정 후 전환 엣지 미표기 — 기존엔 "뒤로가기 · pop"과 "삭제 확정" 두 트리거가 같은 화살표에 뭉쳐 있어 삭제 액션의 존재가 흐름도에서 안 보이던 문제, "삭제·보관 확정 · pop" 엣지를 별도로 분리 표기 (3) SC-09 "네이티브 광고 라벨 표기 규칙" 정책 참조가 ④비즈니스모델상 Phase 4(확장기) 항목인데 Phase 1/3 화면인 SC-09에 앞당겨 붙어있던 불일치 — "없음(MVP 범위)"으로 정정하고 Phase 4 도입 시 별도 정의 필요하다는 단서로 대체. `mermaid_blocks_v2/7_화면흐름도.txt`도 동일하게 갱신
- **와이어프레임 미포함 사유 명문화.** "화면별 상세정의에 와이어프레임 이미지가 보통 들어가지 않냐"는 질문에 — 지금 넣지 말고 3단계 바이브코딩 결과물(실제 작동 화면)로 대체하기로 결정. 화면별 상세정의 섹션 상단에 미포함 이유(텍스트 스펙이 이미 구성요소를 정의해 이중작업, 3단계 결과물이 대체) 1줄 명문화
- **③ 데이터 정의서 초안 작성.** 작성 전 "데이터구조설계 내용이 들어가는 거냐"는 질문에 — 개발 인수인계용 DB 스키마가 아니라 서비스가 다루는 데이터의 종류·출처·관계를 기획자 관점에서 정리하는 문서로 성격을 먼저 정의, AskUserQuestion으로 (1) 속성 타입 표기 수준(개념 수준 vs 개발 인수인계용 — 개념 수준 선택) (2) 데이터 흐름 예시 섹션 포함 여부(①서비스 플로우와 중복돼 제외 선택) 확인 후 작성 진행. ①데이터 객체 정의(사용자/식물/케어이력/날씨데이터/알림/제품 6개 + 파생 데이터로 케어 리포트 별도 표기, 제품 리뷰·구매 트랜잭션은 자체 미보유 데이터로 명시) ②객체별 속성 정의(6개 표, 속성·타입·필수여부·출처·설명) ③데이터 관계도(Mermaid flowchart, 1-N 실선/참조·트리거 점선 구분) 3단 구성. 관계도 코드도 기존 컨벤션대로 `mermaid_blocks_v2/8_데이터관계도.txt`로 순수 코드 파일 분리 전달
- **③ 데이터 정의서 — 영문 필드명 추가.** "속성이 테이블별 필드명(영어)으로 제대로 적혔으면 좋겠다"는 요청에 따라 6개 표 전체에 `필드명` 컬럼(영문 snake_case) 추가, PK·FK 표기(`(PK)`/`(FK)`)도 병기. 영문 필드명을 1:1로 매칭하기 어려운 결합형 한글 속성은 분리 — 날씨 데이터의 "온도·습도·강수량"을 `temperature`/`humidity`/`precipitation` 3개 행으로, 제품의 "이름·썸네일·가격"을 `name`/`thumbnail_url`/`price` 3개 행으로 분리
- **④ 정책정의서 초안 작성.** 작성 전 "정책정의서에 어떤 내용이 들어가야 하는지, 예외·결합 규칙 위주로 빠짐없이 정리해달라"는 요청에 따라 문서 전체에서 "④정책정의서에서 확정 필요"로 표시돼있던 항목(SC-03 알림권한 재요청 문구, SC-07 삭제 시 복구가능여부, SC-09 네이티브광고 라벨, 데이터 관계도 회원탈퇴 시 하위데이터 처리)을 전수 수집해 프레임워크로 제시 — ①정책(운영 기준) ②예외처리 규칙(상황별 대응) ③예외 결합 규칙(여러 예외 동시 발생 시 우선순위) 3단, 사용자가 "표형태로" 요청해 전부 표로 작성. ①정책 12건(데이터 2·알림 3·권한 2·날씨임계값 3·커머스 2, 날씨 임계값 수치와 재알림 횟수·발송시간대·데이터 보존기간 등은 문서에 없던 신규 수치라 합리적 가정으로 채움), ②예외처리 규칙 7건(기존 5개 예외 분기 + 신규 2개: 날씨API 조회실패·제품카탈로그 조회실패, 상황/트리거조건/시스템대응/사용자노출/관련정책 5열 표), ③예외 결합 규칙 3건(위치+알림 권한 동시거부, 날씨변수+알림미실행 동시발생, 알림거부 중 날씨변수 발생 — 사용자가 지적한 "결합 규칙"이 정확히 이 빈틈이었음, 기존 문서엔 예외를 항상 독립적으로만 다뤄 전혀 없던 항목). 작성 후 SC-03·SC-07·SC-09·데이터 관계도의 기존 "확정 필요" 문구도 확정된 정책 내용으로 역참조 갱신해 문서 전체 일관성 확보
- **2단계 산출물 4개 교차 검토 — 문서 간 정합성 + 사용자 시나리오 워크스루.** "상세기획 산출물 검토 시 뭘 봐야 하냐"는 질문에 5개 축(문서 간 정합성/완결성/실현가능성/추적가능성/시나리오 워크스루)을 제시, 이 중 아직 안 해본 정합성·워크스루 2개를 바로 실행. 사용자 시나리오 3개를 화면흐름도·데이터정의서·정책정의서에 대입해 실제로 따라가보는 방식으로 진행해 5건 발견·수정. (1) 분갈이 알림의 화면 진입 경로 누락 — 시나리오2(수현)에서 드러남, 화면 흐름도에 물주기 알림→SC-03 외에 분갈이 알림→SC-07 딥링크 진입 노드·엣지 추가, SC-07 진입경로·공통내비게이션규칙에도 반영 (2) 사용자(User) 엔티티에 프로필 필드 누락 — SC-11이 참조하는 "프로필 정보"가 데이터정의서에 없던 문제, `nickname`·`profile_image_url` 2개 필드 추가 (3) 품절 정책이 화면 설계에 미반영 — 정책정의서엔 품절 배지·비활성화 규정이 있는데 SC-09 상태분기·SC-10 핵심액션엔 없던 문제, 양쪽에 반영 (4) 데이터 관계도에 권한재요청 알림 트리거 관계 누락 — "사용자→알림(권한 미허용 시 트리거)" 점선 엣지 추가, `mermaid_blocks_v2/8_데이터관계도.txt` 동일 갱신 (5) 시나리오1(지민)의 등록 경로 모호 — 온보딩에서 건너뛰었는지 바로 등록했는지 불명확하던 문장을 "온보딩 건너뛰기 → 이후 내 식물 탭에서 등록"으로 명확화. `mermaid_blocks_v2/7_화면흐름도.txt`도 분갈이 알림 노드 반영해 갱신
- **2단계 [확정].** MVP 구현 범위 정리까지 마친 뒤 사용자가 추가 수정 없이 3단계로 진행 요청 — ①~④ 상태를 ✅로 변경, 콘텐츠 문서에도 "## 2단계. 상세기획 [확정]" 표기
- **2단계 [확정] 이후 보완 — 핵심 계산 로직 신설 + plants.json 작성.** "터미널 가기 전에 빠진 거 없는지, 식물 DB는 어디에 채우냐"는 질문에 gap 점검 — 식물 DB 콘텐츠 자체보다 더 근본적으로, "물주기 자동 계산"(1단계 MoSCoW Must①·핵심 차별화 기능)의 실제 계산 공식이 문서 어디에도 숫자로 정의된 적이 없었고 사용자 시나리오3의 "이틀 뒤로 미룰게요" 한 문장만 유일한 단서였다는 점을 발견해 공식화 우선 제안, 사용자 동의로 진행. ③데이터 정의서에 "핵심 계산 로직" 서브섹션 신설 — 물주기: 종별 기본 간격(`base_watering_interval_days`) + 날씨 보정(폭염 -1일·한파 +2일·장마 +2일, 안전범위 ±50%·최소3일), 분갈이: 생장속도별 기본개월(빠름12·보통24·느림36) + 화분크기 보정(대+6·중0·소-6개월) + 계절보정(생장기 3~6월 밖이면 다음 3월로 이동). 시나리오3의 "이틀 뒤로 미룰게요"가 장마 보정(+2일)과 정확히 일치함을 검증해 공식 근거로 제시. 식물 종류 DB는 인기 관엽식물 24종(몬스테라·스투키·산세베리아 등)을 `base_watering_interval_days`·`growth_rate`·`light_condition_default`·`care_tip`(IA 표의 "짧은 케어팁" 요구사항과 연결)·`image_url` 필드로 `plants.json` 파일 작성해 전달, 이미지는 경로만 지정(실제 이미지 소스는 별도 확정 필요). 데이터정의서 필드 설명·정책정의서 예외처리규칙 표에도 상호 참조 추가, ③구현착수순서 step3·CLAUDE.md(폴더구조·care-calc.js 지시문)도 갱신해 "이미 작성된 파일이니 임의로 다른 계산식·필드 만들지 말 것"을 명시
- **핵심 계산 로직에 계절 배율 추가.** "여름엔 짧아지고 겨울엔 길어지는 계절 요인도 넣으면 좋겠다"는 요청(예시: 기준 7일 식물이 여름 5~7일, 겨울 10~14일)에 따라 물주기 공식에 `season_multiplier` 레이어 추가 — 봄 1.0 · 여름 0.8 · 가을 1.0 · 겨울 1.6배, 계절 적용 후 값에 기존 날씨 보정(폭염/한파/장마)을 더하는 2단 구조로 재설계. 안전범위 기준도 `base_watering_interval_days`에서 `seasonal_interval_days`(계절 적용 후 값)로 변경(겨울 1.6배가 기존 ±50% 캡과 충돌해 ±30%로 조정). 기준 7일 식물로 검증 — 여름 6일(폭염 시 5일)·겨울 11일(한파 시 13일)이 사용자 제시 범위(5~7일/10~14일)에 정확히 들어맞음을 확인. CLAUDE.md의 care-calc.js 지시문에도 계절 배율 수치 반영
- **식물 등록 종류 선택 UI 확정.** "종류가 24개로 제한적인데 드롭다운으로 가야 하나" 고민에 AskUserQuestion으로 순수 드롭다운 vs 검색 가능한 콤보박스 제시 — 검색 가능한 콤보박스 선택. 화면설계서 SC-08 "종류 검색창(자동완성)" 문구는 그대로 유지하되, 서버 검색 API가 아니라 `plants.json`을 클라이언트에서 타이핑에 맞춰 필터링하는 가벼운 콤보박스로 구현한다는 점을 명시(구성요소 설명·CLAUDE.md 둘 다 반영) — 기획 스펙과 구현 난이도 우려를 동시에 해소
- **식물 5종 추가(24→29종) + products.json 작성 + 이미지 에셋 소싱.** 사용자가 나비란·콩란·수박페페·오렌지자스민·호프살렘 5종을 `plants.json`에 추가 요청, 기존 스키마 그대로 작성해 반영(문서·CLAUDE.md의 "24종" 표기 전부 29종으로 갱신). 이어서 미작성 상태였던 `products.json`(도구·흙·영양제·살충제·화분 5개 카테고리, 16개 목업 제품)을 데이터정의서 제품(Product) 테이블 필드 그대로 작성 — 단, SC-09/10의 "품절" 배지·비활성화 UI를 시연하려면 저장된 값이 필요한데 기존 제품 테이블엔 재고 관련 필드가 없던 gap을 발견해 `is_sold_out`(불리언) 필드를 신설 추가(제품 테이블에 반영, 제휴사 실시간 재고 연동이 아닌 목업 값임을 명시). 마지막으로 두 파일의 모든 `image_url`/`thumbnail_url`(식물 29 + 제품 16 = 45건)을 리서치 서브에이전트로 소싱 — Wikimedia Commons의 무료 라이선스 사진을 `Special:FilePath` 핫링크로 연결(로컬 이미지 파일 저장 없음). 다만 이 세션의 네트워크 정책상 각 링크의 실제 HTTP 응답을 직접 검증(curl)하지 못해, 검색 결과로 실존이 확인된 파일명만 사용하는 방식으로 대체 — 문서·CLAUDE.md에 "터미널 작업 전 브라우저로 샘플 확인 권장" 안내 추가

</details>

### 3단계. 바이브코딩 제작 (기획자 관점)

| 산출물 | 상태 | 비고 |
|---|---|---|
| MVP 구현 범위 (실현가능성 검토) | ✅ | 2단계 설계 스펙은 목표 설계로 유지, 실제 구현 시 축소할 항목 7건 표로 정리 |
| ① 기술 스택 정의 | ✅ | Next.js + Supabase + Tailwind + Vercel, 전 구간 무료 티어 |
| ② CLAUDE.md (프로젝트 규칙 문서) | ✅ | 실제 코드 저장소용 `CLAUDE.md` 파일로 별도 전달, 문서엔 요약만 |
| ③ 구현 착수 순서 | ✅ | Phase 1~4 기준 11단계, "물주기 핵심 루프 먼저 관통 후 화면 확장" 원칙 |
| ④ 데일리 개발로그 | ✅ | 양식만 정의(진행 중 채워나감), 트러블슈팅·스펙 변경 이력 통합 |
| ⑤ QA 체크리스트 (체크리스트 / 인수기준) | ✅ | 인수기준은 사용자 시나리오 3개 재사용, 체크리스트는 화면·크로스커팅 기준 사전 작성 |
| 실제 프로토타입 개발 | 🔄 | Claude Code로 진행 중 — ③구현 착수 순서 11단계 중 **1~10번 완료**(화면 12개 전부 구현) + SC-03 정보 밀도 개편, 11번 ①실사용 UX 점검 완료(18건 중 17건 반영) + 2차 마무리(밀린 일정 표현), ②파비콘·링크 미리보기 완료. 남은 건 ③반응형 점검 ④배포 마무리 |

**결정 로그**

<details>
<summary>2026-08-20 (6건)</summary>

- **MVP 구현 범위 정의 — 실현가능성 검토 반영.** "포트폴리오 목적이라 돈 들거나 너무 복잡한 과정은 못 한다, 실현가능성을 검토해달라"는 요청에 따라 2단계 전체 스펙(화면설계서·데이터정의서·정책정의서)을 바이브코딩 구현 난이도·비용 관점에서 재검토, High/Medium/OK 3단계로 위험도 분류해 제시. 사용자가 항목별 확정 답변(1) 푸시 알림 — 실제 발송 대신 SC-11 설정 on/off UX만 구현 (2) 알림 발송 트리거 — 서버 스케줄러(cron) 대신 on-demand(앱 진입 시 재계산) (3) 식물 종류 검색 — 외부 DB 대신 인기 관엽식물 20~30종 하드코딩 JSON (4) 제품 카탈로그 — 목업 데이터 (5) 로그인 — 이메일 로그인만 (6) 사진 — 식물은 종류별 기본 이미지, 프로필은 기본 아바타 고정, 실제 업로드 미구현(이미지 업로드를 이렇게 처리하는 게 어떠냐는 사용자 질문에 타당하다고 답변 후 반영) (7) 정책정의서 세부 수치 — 문서상 정책으로만 명시, 프로토타입 미구현. "3단계. 바이브코딩 제작" 상단에 성격·원칙(2단계 스펙은 목표 설계로 유지, 구현 시 축소 범위만 별도 기록) + 7개 항목 표(설계 스펙/MVP 구현 범위/사유)로 문서화, 데이터정의서의 `photo_url`·`profile_image_url` 행에도 이 표를 참조하는 각주 추가
- **3단계 제목 리스트 확정.** "본격 터미널 작업 전에 뭐가 필요한지 제목 리스트부터 정리하자, 기술스택정의·CLAUDE.md·데일리개발로그·QA체크리스트 등"이라는 요청에 진행 전/진행 중 2그룹으로 나눠 제안. 트러블슈팅 로그·스펙 변경 이력을 별도 문서로 뺄지 질문 — 사용자가 "데일리개발로그에 녹이자"고 확정. 최종 5개 산출물로 3단계 구조 확정: ①기술 스택 정의(프론트/백엔드·DB/외부API/배포플랫폼 + 선정근거) ②CLAUDE.md(프로젝트 규칙 문서 — 폴더구조/컨벤션/제약사항/기획문서 참조방식) ③구현 착수 순서(MVP 로드맵 Phase1 기준 개발순서) ④데일리 개발로그(세션별 진행상황 + 트러블슈팅·스펙변경이력 통합) ⑤QA 체크리스트(기존 계획, 체크리스트/인수기준). 콘텐츠 문서·spec.md 로드맵 표 모두 5개 산출물 구조로 갱신, 기존 "3단계 ①→QA체크리스트"만 있던 제목-소제목 정리 결정(2026-08-19)을 대체
- **① 기술 스택 정의 작성.** "MVP 구현 범위" 표를 직접 근거로 스택 결정 — 프론트엔드 Next.js·스타일링 Tailwind·백엔드/DB/인증 Supabase(이메일 로그인만, 데이터정의서 PK/FK 구조와 매칭)·날씨 OpenWeatherMap 무료 티어(v1.0 `lib/weather.js` 로직 재사용)·식물종류/제품카탈로그는 DB 대신 코드 내 JSON·배포 Vercel·PWA는 next-pwa 최소 설정·상태관리는 라이브러리 없이 React 기본 state. 전부 무료 티어 조합, 실제 푸시 인프라·이미지 스토리지·서버 스케줄러는 "MVP 구현 범위" 표에 맞춰 제외
- **② CLAUDE.md 작성 — 실제 파일로 별도 전달.** CLAUDE.md는 코드 저장소 루트에 두는 실제 프로젝트 규칙 파일이라 노션 문서와 별개로 `/home/claude/CLAUDE.md` 작성 후 SendUserFile로 전달, 콘텐츠 문서 ②엔 성격·구성·핵심역할 요약만 남김. 내용: 프로젝트 개요(기획 문서 2개를 단일 소스로 우선 참조하라는 지시 포함) · 기술 스택(①과 동일) · 폴더 구조 제안 · 데이터 모델·화면ID 참조 규칙(데이터정의서·화면설계서의 필드명/화면ID를 임의 변경 금지) · "MVP 구현 범위 — 반드시 지킬 것"(3단계 상단 표를 AI 코딩 도구용 지시문으로 재정리, 위치·알림 권한 폴백 등 Must-have 로직은 구현 대상이나 세부 수치만 단순화한다는 점도 명시) · 코딩 컨벤션 · 하지 말아야 할 것. AI 코딩 도구가 기획 스코프를 임의로 넘어서지 않게 하는 가드레일 역할이 핵심
- **CLAUDE.md·3단계 5개 산출물 검토 — 5개 축.** "CLAUDE.md랑 3단계 산출물 검토하려면 뭘 봐야 하냐"는 질문에 이 단계 특성에 맞는 축 5개(CLAUDE.md 자체 실효성/CLAUDE.md·콘텐츠문서 일치/③구현착수순서 실행가능성/⑤QA체크리스트·MVP범위 정합성/앞단 2단계 참조 정확성) 제시, 전부 실행. 발견 3건 모두 반영. (1) ③step2 "사용자는 Supabase Auth가 기본 처리"가 부정확 — 데이터정의서 User 엔티티의 `location`/`notification_permission`/`nickname`/`profile_image_url`은 `auth.users` 기본 테이블에 없는 앱 전용 필드라 `auth.users`와 1:1 연결된 별도 `profiles` 테이블 필요, "4개 테이블"을 "5개 테이블"로 정정하고 CLAUDE.md 데이터 모델 섹션에도 동일 원칙 추가 (2) ③step9에 Phase 라벨 없이 섞여 있던 "케어 리포트"가 MVP로드맵상 명확히 Phase 3 항목인데 Phase2 직후처럼 읽히던 문제 — step10(제품탭, Phase3)으로 이동해 "제품 탭 · 케어 리포트 (Phase 3, 여유 시)"로 재구성 (3) CLAUDE.md에 테스트 작성 여부·에러 핸들링 원칙·환경변수 관리 원칙이 없어 AI 코딩 도구가 임의 판단할 여지가 있던 문제 — "테스트 · 에러 처리 · 환경변수" 섹션 신설(테스트 코드 미작성·QA체크리스트로 대체, 에러 시 화면별 상세정의 상태분기 우선 참조, `.env.local`+`.gitignore` 원칙). 축2(CLAUDE.md↔콘텐츠문서 기술스택·MVP범위 일치)·축4(QA체크리스트에 스코프 밖 항목 없음, 제품탭 체크 의도적 누락 확인)는 이상 없음 확인
- **③④⑤ 일괄 작성.** "3개 다 같이 진행해달라, 뒤 2개는 진행 중에 쌓는 거니까 양식만 넣으면 되나"는 질문에 — 데일리 개발로그는 양식만으로 충분하다고 답변했지만, QA 체크리스트는 다르다고 판단해 차별화. 화면별 상세정의에 이미 "사용자 액션과 결과"·"상태 분기"가 정의돼 있어 그 내용을 인수기준·체크리스트로 미리 뽑아낼 수 있다는 점을 짚고 실제 초안까지 작성. ③구현 착수 순서 — MVP 로드맵 Phase 순서를 그대로 개발 순서에 적용, "물주기 핵심 루프(등록→계산→기록→확인)를 먼저 끝까지 관통시키고 화면을 옆으로 넓히는" 원칙으로 11단계 작성(Phase1 우선, Phase3 제품탭은 여유 시). ④데일리 개발로그 — 목표/진행작업/트러블슈팅/스펙변경/내일할일 5항목 양식 + 예시 1건(형식 참고용, 실제 기록 아님을 명시). ⑤QA 체크리스트 — 인수기준은 2단계 ①의 사용자 시나리오 3개를 그대로 재사용(시나리오1→Phase1, 시나리오2→Phase2, 시나리오3→날씨예외), 체크리스트는 인증·온보딩/오늘탭/내식물/캘린더/마이페이지/크로스커팅 6개 카테고리로 화면별 상세정의·정책정의서 예외결합규칙 기반 체크박스 항목 사전 작성

</details>

<details>
<summary>2026-08-21 (15건)</summary>

- **③구현 착수 순서 1번 — 프로젝트 셋업 완료.** Next.js 16.3.1(App Router·TypeScript) + Tailwind v4 + ESLint 생성, 폴더 구조를 CLAUDE.md 기준(`/app` `/components` `/lib` `/data` `/types`)으로 정리하고 기존 `plants.json`·`products.json`을 `/data`로, 이미지 에셋을 `public/images/`로 이동. Supabase 프로젝트 `sikjipsa`(ref `ebtajqfrxfcisnhezphx`·ap-northeast-2) 생성 후 `lib/supabase.ts` 클라이언트 작성, 키는 `.env.local`(gitignore)·`.env.local.example`(커밋) 분리. GitHub private 저장소 `jungbin9999/sikjipsa` 생성·푸시 후 Vercel Import로 배포 파이프라인 연결, 빈 화면 배포로 파이프라인 검증 완료(https://sikjipsa-one.vercel.app). 트러블슈팅 4건 — (1) 프로젝트 디렉터리명이 한글이라 `create-next-app`이 npm 이름 제약에 걸려 임시 폴더 생성 후 이동, package name은 `sikjipsa` (2) 상위 홈 디렉터리 lockfile을 Turbopack 루트로 잡는 경고 → `next.config.ts`에 `turbopack.root` 고정 (3) `sikjipsa.vercel.app`이 백지로 떠 빌드 실패로 오판했으나 **타인이 선점한 동명 도메인**이었음, 이 프로젝트 URL은 `-jungvin` 접미사가 붙는 쪽 (4) Vercel Authentication(Standard Protection)이 신규 프로젝트 기본 on이라 로그인 사용자만 접속 가능 → 포트폴리오 공개용이므로 해제
- **③구현 착수 순서 2번 — 데이터 모델 구축, 제품 테이블은 미생성으로 확정.** 착수 전 문서 충돌 발견 — ③구현 착수 순서 2번은 "profiles·식물·케어이력·알림·제품 5개 테이블", CLAUDE.md 기술 스택 표는 "식물 종류·제품 카탈로그 = 코드 내 JSON(DB 테이블 아님)". 제휴사 API 연동 금지·목업 사용이 MVP 구현 범위 확정 사항이므로 제품은 `/data/products.json` 단일 소스로 가고 테이블은 만들지 않는 쪽으로 사용자 확정(테이블을 만들어도 쓰이지 않고 JSON과 이중 관리가 되는 점이 근거). 최종 4개 테이블 — `profiles`(`auth.users`와 1:1, `login_account`는 `auth.users.email`이 이미 보유하므로 미보유) · `plants` · `care_logs` · `notifications`, 데이터정의서 필드명·타입 그대로 매핑. 데이터정의서에 열거값이 정의된 `light_condition`(직사광·간접광·그늘) · `status`(활성·보관·삭제) · `care_type`(물주기·분갈이)은 CHECK 제약으로 강제. 전 테이블 RLS 활성화(본인 데이터만, `care_logs`는 `plants` 조인으로 소유권 판정), 사용자 삭제 시 식물→케어이력까지 cascade. 데이터정의서에 없는 추가 요소로 회원가입 트리거(`handle_new_user`)를 넣었는데, 이는 CLAUDE.md가 요구하는 `auth.users` ↔ `profiles` 1:1 연결을 보장하기 위한 구현 수단. 마이그레이션은 `supabase/migrations/20260821000000_init_schema.sql`로 버전 관리
- **③구현 착수 순서 3번 — 정적 데이터 검수 완료, 이미지 링크 전량 교체.** `plants.json` 29종·`products.json` 16개를 데이터정의서 필드 기준으로 스키마 검증 — 필드 누락·타입 오류·중복 ID·열거값(`growth_rate`/`light_condition_default`) 위반 0건. 이미지 링크 45건은 전부 살아 있었으나(HTTP 200 + 이미지 MIME) 두 가지 문제를 발견해 사용자 확인 후 수정. (1) `Special:FilePath` 방식이 이미지 1장당 리다이렉트 2번을 타서 45장 = 135요청이 되고 curl 기준 45건 중 36건이 429(레이트리밋) — 최종 CDN 주소(`upload.wikimedia.org`의 500px 썸네일)로 전량 교체해 1장당 1요청으로 축소, 브라우저 렌더도 45/45 즉시 로드 확인 (2) 품목과 사진이 안 맞는 5건 교체 — 특히 **호프살렘(필로덴드론)에 켄차야자 사진이 들어가 있던 종 오류**, 그 외 몬스테라(열매 클로즈업→잎)·배양토(버섯 사진→흙)·액체비료(알갱이 비료→액상 제품)·디자인 화분 화이트(테라코타 갈색→백색 화분). 교체 후보는 Commons API 검색 결과를 브라우저에 격자로 띄워 눈으로 고르는 방식으로 선정. 종류·필드·값은 손대지 않고 이미지 주소만 변경(CLAUDE.md의 두 파일 임의 변경 금지 조항 때문에 착수 전 사용자 확정). `next.config.ts`에 `images.remotePatterns`로 `upload.wikimedia.org` 허용 추가(추후 `next/image` 사용 대비)
- **③구현 착수 순서 4번 — SC-01 회원가입·로그인 구현.** Supabase Auth 이메일 인증으로 SC-01 구현(`app/sc01/page.tsx`, 로그인/회원가입 토글을 한 화면에 배치). 전환은 화면 흐름도 표기 그대로 — 회원가입 → SC-02 push, 기존 회원 로그인 → SC-03 replace, 로그아웃 → SC-01 replace. 진입점(`/`)은 세션 유무로 SC-01·SC-03 분기. **이메일 확인 메일은 사용자 결정으로 비활성화**(`supabase config push`로 원격 auth 설정 반영, 가입 즉시 세션 발급) — 포트폴리오 프로토타입이라 가입할 때마다 메일함을 여는 비용이 불필요. 실기기 검증: 오류 비밀번호 → 인라인 에러, 재시도 로그인 성공 → SC-03, 신규 가입 → SC-02, 로그아웃 → SC-01, 360·390·430px 레이아웃 정상. SC-02·SC-03은 흐름 검증용 임시 플레이스홀더(5·6번에서 교체), 로그아웃 버튼도 SC-12 구현 전까지 SC-03에 임시 배치. 미해결로 남긴 것 — SC-01 에러 문구는 화면설계서에 정의가 없어 임시 문구를 `ERROR_MESSAGES` 한 곳에 모아둠(확정 필요). 부수 처리 2건: `next dev`가 실행 때마다 `CLAUDE.md`에 자체 블록을 덧붙여 `agentRules: false`로 차단, 인증 에러 매칭을 메시지 문자열에서 `error_code` 기준으로 변경(실제 응답에 마침표가 붙어 문자열 매칭이 실패했음)
- **UX/UI 디자인 방향 확정 + 토큰화.** "디자인 파트 들어가기 전에 참고 이미지를 반영하고 싶다"는 요청에, 참고 자료를 언제까지 받아야 하는지부터 정리 — 5번(온보딩·등록)에서 버튼·입력·카드·리스트·탭바 같은 재사용 컴포넌트가 처음 만들어지므로 그 전이 실질 마감이고, 늦어도 6번(SC-03) 전이라는 점을 근거와 함께 제시. 사용자가 파일명으로 참고 용도를 구분하는 방식(`typo_*`·`color_*`·`design_*`)을 제안해 그대로 채택, `/design-refs`에 이미지 6장 수집. 이미지 분석 결과 공통 방향은 라임+블랙+오프화이트 3색·큰 라운드 카드·굵은 헤드라인/얇은 라벨·pill 버튼으로 정리. 결정 2건 — (1) 배경 톤은 **라이트 기본 + 입구 화면(SC-01·SC-02)만 다크**(식물 사진 29종이 전면에 깔리는 앱이라 사진 가독성 우선) (2) 색은 사용자가 지정한 5색 팔레트(`#C9F075`·`#F6F6F6`·`#FFFFFF`·`#08080A`·`#CCBCF6`, 라벤더는 소형 포인트 한정)를 단일 소스로 확정. `app/globals.css`의 `@theme`에 토큰화하고 CLAUDE.md에 "디자인" 섹션을 신설해 이후 화면이 임의 색을 만들지 못하게 고정. 접근성 규칙 1건 추가 — 라임 위 텍스트는 항상 `ink`(흰 텍스트는 대비 미달). 팔레트에 없는 에러 색은 기능색으로 2개(`danger`·`danger-soft`) 별도 정의하고 그 사실을 명시. SC-01은 기존 emerald 임시 스타일을 새 팔레트로 교체
- **③구현 착수 순서 5번 — 온보딩·등록 흐름(SC-02·SC-08·SC-06) 구현.** 먼저 공통 기반부터 — `types/index.ts`에 데이터정의서 엔티티 타입 6종, `lib/care-calc.ts`에 물주기·분갈이 계산, `lib/plants.ts`에 종류 정적 데이터 헬퍼. `care-calc.ts`는 데이터정의서 "핵심 계산 로직"의 공식(계절 배율 → 날씨 보정 → ±30%·최소 3일 안전범위, 분갈이는 생장속도+화분크기 보정 후 생장기 밖이면 다음 3월로 이동)을 그대로 구현하고, 문서에 적힌 검증 예시 6건과 대조해 전부 일치하는 것을 확인한 뒤 화면 작업에 들어감. **SC-02 온보딩** — 위치 권한 → 알림 권한 → 첫 식물 등록 유도 3스텝, 위치 거부·실패 시 서울 기준값으로 폴백해 `profiles.location`에 저장(정책정의서 예외처리 규칙), 알림 권한 결과는 `notification_permission`에 저장. **SC-08 식물 등록** — 종류 선택은 `plants.json` 29종을 클라이언트에서 필터링하는 콤보박스, 선택하면 같은 화면 안에서 정보 입력 단계로 전환(화면설계서 "같은 화면 내 스텝 전환"), 등록 시 `next_watering_date`·`next_repotting_date`를 계산해 저장하고 `care_logs` 2건(물주기·분갈이 예정)을 함께 생성해 "케어 캘린더 자동 생성"을 구현, 필수값 누락 시 인라인 경고. **SC-06 식물 리스트** — 식물명·애칭 검색, 카드에 사진·애칭·종류·케어팁, 빈 상태 안내. 실기기 검증 — 온보딩 3스텝(위치 건너뛰기 → 서울 폴백 문구 노출) → 등록(필수값 경고 확인 후 몬스테라 등록) → 리스트 카드 노출까지 통과, DB에서 `next_watering_date` 2026-08-27(7일×여름 0.8=6일)·`next_repotting_date` 2029-03-01(24개월 뒤가 8월이라 다음 3월로 이동)·`care_logs` 2건·`profiles.location` "서울"까지 계산대로 저장된 것 확인. 위치 권한 저장 형식은 허용 시 "위도,경도" 문자열·거부 시 "서울"로 한 필드에 담기로 결정(데이터정의서 `location`이 텍스트 단일 필드라 별도 컬럼을 만들지 않음). SC-04·SC-07은 아직 임시 플레이스홀더
- **③구현 착수 순서 6번 — 핵심 가치 화면(SC-03·SC-04·SC-07) 구현, Phase 1 물주기 루프 관통.** 날씨는 `app/api/weather/route.ts`를 거쳐 조회 — OpenWeatherMap 키를 클라이언트에 노출하지 않기 위해 서버 라우트를 두고 같은 좌표는 10분 캐시(무료 티어 호출 절약). `lib/care-service.ts`에 화면 진입 시 재계산·오늘 할일 조회·완료 처리를 모아 서버 스케줄러 없이 on-demand로 동작시킴(MVP 구현 범위). **SC-03** — 날씨 배너 · 케어현황 링 3개 · 오늘의 할일 카드와 완료 체크 · 알림 권한 거부 시 재요청 배너(세션당 1회, 정책정의서 "권한 재요청 빈도"). 진입 시 물주기 예정일을 재계산하고 미완료 `care_logs`의 예정일까지 맞춘다. **SC-04** — 등록 식물 0개면 SC-03에서 replace로 대체. **SC-07** — 물주기 기준·이력 조회와 보관/삭제(확인 모달, 정책상 삭제도 보관 처리가 기본), 분갈이 이력·배치 위치 섹션은 7번 몫이라 제외. 하단 탭바 5개를 붙이고 미구현 탭(SC-05·SC-09·SC-11)은 플레이스홀더로 연결. 실기기 검증 — 물주기가 밀린 상태를 만들어 진입하니 예정일이 2026-08-11로 재계산되고 할일 카드에 노출, 완료 체크 시 링이 0/1→1/1로 즉시 갱신되고 DB에서 `last_watered_at` 오늘·다음 예정일 08-27·다음 회차 `care_logs` 생성까지 확인. 날씨는 키 미발급 상태라 "날씨 정보를 불러오지 못했어요" 폴백 경로로만 검증(물주기 계산은 정상 동작, 상태 분기 정의대로). 문서에 정의가 없어 임의로 정한 것 3건 — 케어현황 링 3개의 의미(오늘 처리 대상 중 완료 비율 / 종별 권장 광량과 실제 배치 일치 비율), 장마 판정의 시간당 강수량 환산(20mm/24h ≈ 0.83mm/h, "3일 연속 강수"는 이력이 필요해 미구현), 날씨 예외 동시 충족 시 우선순위(기온 예외 먼저). 전부 확정 필요 항목으로 기록
- **날씨 처리 방식 결정 — 실제 API 유지 + 시연 시나리오 스위치.** "날씨를 목업으로 가면 어떠냐"는 질문에 장단점을 정리해 제시 — 목업의 실질 이점은 시연 안정성보다 **원하는 날씨를 원할 때 보여줄 수 있다는 점**이고(실제 API로는 8월에 한파를 못 만든다), 반대로 날씨는 제품 카탈로그와 달리 MVP 구현 범위 표에서 목업으로 지정된 적이 없고 기술 스택 정의·구현 착수 순서 6번에 "OpenWeatherMap 연동"으로 확정돼 있어 목업 전환은 스펙 변경이며 서비스 핵심 차별점("날씨를 아는")을 약화시킨다는 점을 짚음. 사용자가 3안(실제 API 유지 + 시연 스위치) 선택. `lib/weather.ts`에 `WEATHER_SCENARIOS`(맑음·폭염·한파·장마)를 두고 `/sc03?weather=장마` 형태로 강제, 이때만 API를 건너뛰고 배너에 "시연" 배지를 붙여 실제 조회값과 구분. 부수 효과로 QA 체크리스트 인수기준의 사용자 시나리오 3(영빈 — 장마철 물주기 연기)이 검증 가능해짐 — 실제로 장마 시나리오 진입 시 예정일이 2026-08-27 → 08-29로 이틀 밀리는 것을 확인해 시나리오 문장과 일치함을 재확인. 앞서 날씨 API 장애 대비도 함께 보강(3초 타임아웃·날씨와 식물 병렬 조회·폴백 배너를 정상 배너와 같은 모양으로 통일해 실패 시 레이아웃 점프 제거)
- **③구현 착수 순서 7번 — 분갈이·위치 확장(SC-07) + 컬러 정체성 보강.** SC-07을 화면설계서의 "탭형 하위 섹션" 정의대로 물주기 / 분갈이 / 배치 위치 3섹션으로 재구성(화면 이동 없이 데이터만 교체). 분갈이 섹션은 다음 권장 시기 D-day·이력·화분 크기 변경을 담고, 크기를 바꾸면 분갈이 주기 보정값이 달라지므로 예정일과 미완료 예정 이력을 함께 재계산. 배치 위치는 인라인 수정 후 즉시 저장하며, 종별 권장 광량과 일치하면 라임·다르면 라벤더 카드로 구분해 "이 자리가 맞는지"를 색으로 판단하게 함. 분갈이 완료 처리도 추가 — 다음 분갈이는 입양일이 아니라 이번 분갈이 날짜를 기준으로 다시 계산(데이터정의서 공식의 기준일 해석을 확장). 함께 **컬러 정체성 보강** — "흰/회색 비중이 높아 식집사만의 색이 안 보인다"는 피드백에 따라 참고 이미지 `typo_03`의 리스트 패턴(흰·블랙·라임 카드를 번갈아 배치)을 도입, `lib/tone.ts`에 톤 로테이션 헬퍼를 두고 SC-03 할일 카드·SC-06 식물 카드에 적용. SC-04 빈 상태와 SC-03 완료 상태도 라임 블록으로, SC-07 프로필은 블랙 카드 + 라임 D-day 배지로 교체. 라임 위 텍스트는 항상 ink라는 기존 규칙은 톤 헬퍼가 카드별 보조 텍스트 색까지 함께 반환하도록 해서 강제. 실기기 검증 — 3개 탭 전환, 배치 위치를 그늘로 바꾸자 카드가 라벤더로 바뀌며 "몬스테라는 간접광을 더 좋아해요" 안내, SC-03 화분위치 링도 2/2 → 1/2로 즉시 반영
- **③구현 착수 순서 8번 — SC-05 월간 캘린더 뷰 구현.** 월 이동 화살표 · 날짜별 배지 · 월간/주간 뷰 전환 토글 · 날짜 선택 시 하단 당일 리스트 갱신 · 리스트에서 완료 체크까지 화면설계서 구성 요소 그대로 구현(완료 처리는 SC-03과 같은 `completeCareItem` 재사용). 날짜 격자 생성·범위 이동·라벨 포맷은 `lib/calendar.ts`로 분리. 검증 중 발견해 고친 것 2건 — (1) 주간 뷰로 전환하면 선택 날짜가 보이는 범위 밖으로 나가 하단 리스트가 엉뚱하게 비어 보이던 문제, 범위가 바뀔 때 선택 날짜를 오늘(또는 범위 첫날)로 되돌리도록 보정 (2) 선택 날짜와 오늘이 같은 스타일이라 오늘을 알 수 없던 문제, 오늘 칸에 라임 링 추가. 문서에 정의가 없거나 무료 티어 제약으로 좁힌 것 2건 — "날짜별 날씨 아이콘"은 오늘 칸에만 표시(과거·미래 일별 날씨는 유료 History API가 필요, 유료 API 금지 제약과 충돌), "날짜별 식물 배지(색상 구분)"의 구분 기준이 정의되지 않아 케어 유형(물주기 라임·분갈이 라벤더·완료 회색)으로 임의 구분 — 확정 필요
- **③구현 착수 순서 9번 — 마이페이지(SC-11·SC-12) 구현.** SC-11은 프로필 영역(닉네임 인라인 수정, 사진은 MVP 범위대로 기본 아바타 고정, 미입력 시 이메일 앞부분 표시) · 위치(날씨) 설정(현재 위치 / 서울 기준 재설정) · 알림 설정 on/off 토글(실제 발송 없이 상태만 저장, MVP 구현 범위) · 계정 관리 진입으로 구성. 정책정의서 예외 결합 규칙대로 위치·알림 권한이 모두 거부 상태면 "서울 기준값 사용 중"·"알림 꺼짐" 배지를 나란히 노출. SC-12는 로그아웃과 회원탈퇴(확인 모달). 위치 요청 로직이 SC-02와 중복이라 `lib/location.ts`로 분리해 양쪽에서 재사용. 케어 리포트 카드는 MVP 로드맵상 Phase 3라 10번으로 미룸. 범위를 좁힌 것 — 회원탈퇴 시 계정(`auth.users`) 자체 삭제는 service_role 키가 필요해 미구현(등록 식물을 '삭제' 상태로 바꾸고 로그아웃까지만), 30일 유예 파기도 스케줄러가 필요해 MVP 범위 밖. 같은 세션에서 SC-06 카드 구조도 피드백대로 재조정 — 카드는 흰색으로 통일하고 우측 정보를 두 개의 분리된 블록으로(마지막 물준 날짜는 그레이, 다음 예정일은 다크, 물 줄 당일부터 라임). 식물별 알림 on/off는 데이터정의서에 없는 필드가 필요해 사용자 판단으로 보류
- **③구현 착수 순서 10번 — 제품 탭·케어 리포트(Phase 3) 구현.** 여유 시 항목이었으나 사용자 요청으로 진행. **SC-09** — 카테고리 필터 칩(전체+5종, 화면 전환 없이 필터링) · 2열 제품 그리드 · 품절 배지와 카드 선택 비활성화(정책정의서 "품절·판매중단 처리") · 분갈이 시기가 30일 이내인 식물이 있으면 화분·흙 추천 섹션을 상단 고정(화면설계서 "분갈이 알림 연동 추천"). **SC-10** — 제품 정보·가격·리뷰 요약, 제휴 링크는 새 탭으로 이동(`target="_blank"`), 품절이면 버튼 비활성화. **SC-11 케어 리포트 카드** — 데이터정의서상 저장 엔티티가 아니라 케어 이력 집계값이므로 `lib/care-report.ts`에서 표시 시점에 산출. SC-07 분갈이 탭에도 시기가 다가오면 제품 탭으로 보내는 배너를 추가해 화면 흐름도의 SC-07 → SC-09 tab전환 경로를 채움. 문서에 정의가 없어 임의로 정한 것 — 케어 리포트 지표 2종의 계산 기준(주간 완료율 = 최근 7일 예정 항목 중 완료 비율, 연속 관리일수 = 완료 기록이 끊기지 않은 일수, 오늘 아직 안 했어도 어제까지 이어졌으면 유지). 미구현 — SC-09의 "카탈로그 조회 실패" 에러 분기는 제품이 코드 내 JSON이라 발생할 수 없어 제외. 이로써 화면 12개가 모두 구현됨
- **SC-03 오늘 탭 정보 밀도 개편.** "메인 탭인데 정보량이 너무 적다, 현재 날씨에 따른 관리와 식물별 설정 차이가 안 보인다"는 피드백. 착수 전 두 가지를 확정 — 식물 선택 탭은 **"전체" 칩 + 식물별 전환** 방식(할일 필터가 아니라 하단 전체가 바뀜), 날씨 안내는 **날씨 상태별 일반 팁**(식물별 맞춤 아님). 재구성 결과 — 헤더에 오늘 날짜, 날씨 카드 확장(습도·강수 지표 분할 + 날씨 팁 + 기존 일정 보정 안내), 케어현황 링 유지, 식물 선택 가로 칩(처리할 항목이 있는 식물엔 라임 점), 하단은 선택에 따라 전환(전체 = 오늘의 할일 + 다가오는 일정 / 식물별 = 물주기·분갈이·배치 상태 카드 + 종별 케어팁 + 상세 이동). 날씨 키가 들어오면 분량이 늘어나는 점을 고려해 폴백 카드도 정상 카드와 같은 다크 카드로 잡아 레이아웃이 흔들리지 않게 함. 검증 — `?weather=폭염` 진입 시 팁·보정 안내가 함께 뜨고 식물별 뷰의 다음 물주기가 D-6에서 D-5로 당겨진 것(폭염 -1일)까지 확인, 배치가 종별 권장과 다른 몬스테라는 라벤더 카드로 구분됨. 화면설계서 SC-03 구성요소에 없는 요소 3개(식물 선택 탭·다가오는 일정·날씨 팁)를 추가한 것이므로 확정 필요 항목으로 기록
- **PWA 제외 결정 — 기술 스택 변경.** UX 다듬기를 11번 전에 할지 후에 할지 묻는 질문에서 시작해, PWA의 실효를 따져본 끝에 **아예 제외**하기로 확정. 사용자가 "면접관은 홈 화면에 설치하지 않는데 왜 하냐"고 정확히 짚었고, 실제로 PWA의 3가지 효과(홈 화면 아이콘·주소창 없는 전체화면·오프라인)는 전부 **설치한 사람에게만** 발생해 포트폴리오 열람자에겐 가치가 없다. 여기에 (1) 식집사는 서버 데이터에 의존해 오프라인이면 껍데기만 뜨고 (2) 기술 스택에 명시한 `next-pwa`가 2022-08 이후 방치돼 App Router 이전 시점에 멈춘 패키지라 현재 Next 16에서 쓸 수 없다는 점(확인 완료, 대안은 `@serwist/next`)이 겹침. 대신 **모든 열람자가 반드시 보는데도 비어 있던 두 가지**를 11번으로 옮김 — 브라우저 탭 파비콘이 아직 Next.js 기본 아이콘이고, 링크 공유 미리보기(Open Graph) 설정이 아예 없어 URL을 공유해도 썸네일 카드가 뜨지 않는 상태였음. 기술 스택 정의·구현 착수 순서 11번·QA 체크리스트·CLAUDE.md 기술 스택 표를 모두 갱신
- **④데일리 개발로그 실제 기록 시작.** 지금까지 양식만 정의돼 있던 데일리 개발로그를 `docs/dev-log.md`로 코드 저장소에 생성하고 2026-08-21 항목(목표/진행작업/트러블슈팅 5건/스펙과 다르게 구현한 부분 4건/내일 할 일)을 채움. 문서에 경로가 명시된 적은 없어 `docs/` 아래로 결정. 스펙과 다르게 구현한 부분은 나중에 "데이터정의서엔 없는 게 왜 DB에 있지?"로 헷갈릴 지점이라 이유까지 같이 기록하는 방식으로 운용

</details>

<details>
<summary>2026-08-23 (4건)</summary>

- **PWA 제외 확정 — 기술 스택 변경.** UX 다듬기를 11번 전후 어디서 할지 묻는 질문에서 출발해, 사용자가 "면접관은 홈 화면에 설치하지 않는데 왜 하냐"고 짚은 것을 계기로 PWA의 실효를 재검토. 설치·전체화면·오프라인 세 효과가 **모두 설치한 사람에게만** 발생해 포트폴리오 열람자에겐 가치가 없고, 서버 데이터에 의존하는 앱이라 오프라인이면 껍데기만 뜬다는 점, 기술 스택에 명시한 `next-pwa`가 2022-08 이후 방치돼 Next 16에서 쓸 수 없다는 점(확인 완료, 대안 `@serwist/next`)이 겹쳐 **제외**로 확정. 대신 **모든 열람자가 보는데도 비어 있던** 브라우저 탭 파비콘(아직 Next.js 기본 아이콘)과 링크 공유 미리보기(Open Graph 미설정)를 11번으로 편입. 기술 스택 정의·구현 착수 순서 11번·QA 체크리스트·CLAUDE.md를 모두 갱신
- **③구현 착수 순서 11번 ① — 실사용 UX 점검 수행, 18건 중 17건 반영.** 브라우저 자동화로 SC-01~SC-12를 순회하며 화면설계서의 구성 요소·상태 분기를 대조하고 사용자 시나리오 1·3을 재현. 치명적 1건(SC-05에서 날씨 아이콘이 오늘 날짜 숫자를 대체해 달력에서 오늘을 못 읽음)을 포함해 접근성(보조 텍스트 대비 WCAG AA 미달 74곳 → `/60` 상향), 모바일(터치 타겟 44px), 시각 일관성(탭바 아이콘을 이모지·기호 혼용에서 선 아이콘 5종으로 통일), 마이크로카피(위치 폴백 사유 미설명) 등을 수정. 미반영 1건은 SC-03 정보 위계(날씨가 할 일보다 먼저 읽히는 것)로, 서비스 컨셉상 **의도된 배치**로 확정. 점검 중 "습도 100%인데 팁이 기본값"이라는 실제 데이터 문제를 발견해 고습(80%↑) 분기를 추가한 것도 이때
- **케어현황 링 지표 확정 + 날씨 파트 강조.** 사용자가 "어떤 완료율인지 직관적으로 파악이 어려워 불필요하게 느껴졌다"고 지적. 원인은 링이 아니라 지표 정의였음 — "오늘 처리 대상 중 완료 비율"이라 할 일이 없는 날은 분모가 0이라 늘 "—"였다. **"전체 식물 중 지금 문제없는 식물 수"**로 세 링의 기준을 통일하고 섹션 제목과 링별 상태 문구를 붙여 해결(확정 필요 목록에서 해제). 함께 "날씨 기반이 서비스의 가장 중요한 포인트"라는 요청에 따라 SC-03 날씨 블록을 강조 — 아이콘·온도 확대, 관리 팁 승격, **날씨가 일정에 반영된 결과를 항상 한 줄로 노출**(기존에는 특이기상일 때만 나와 평소엔 연결이 보이지 않았음). 블랙/라임을 위아래로 나눈 첫 시안이 "이분법적"이라는 피드백을 받아, 색으로 면을 가르지 않고 라임을 강조색으로만 쓰는 방식으로 재설계
- **오늘의 할일 완료 표현 확정.** "완료하면 바로 사라져서 뭘 했는지 안 보인다, 실수로 누를 수도 있다"는 지적에 따라 완료 항목을 목록에 남기고 **되돌리기**를 추가(`undoCareItem` — 다음 회차 이력 삭제와 `last_watered_at` 복원까지 처리). 상태 표현은 세 차례 피드백을 거쳐 확정 — 취소선 대신 불투명도, 할 일은 블랙 카드+연두 원 / 끝낸 일은 흐린 흰 카드+그레이 원, 버튼 안은 **글자로 통일**(완료 / 취소). 아울러 "체크하지 않고 하루가 지나면 다음 날에도 남아야 한다"는 요구는 조회 조건이 이미 그렇게 동작함을 확인해 별도 수정 없이 검증만 함

</details>

<details>
<summary>2026-08-25 (2건)</summary>

- **③구현 착수 순서 11번 ① 2차 마무리 — "밀린 일정"을 일급 상태로 승격.** 1차 점검에서 남겨둔 수정분을 마무리하며 공통 원인을 하나 찾음 — 지난 일정을 전부 **예정일 날짜 그대로**(`분갈이 · 2026-03-01`) 보여주고 있어서, 사용자가 날짜를 오늘과 직접 비교하기 전까지 밀렸다는 사실 자체를 알 수 없었다. 세 화면에 걸쳐 "얼마나 밀렸나"로 표현을 통일 — (1) SC-03 할일 카드에 `n일 지남`을 라임 강조로, 90일 넘게 밀리면 세 자리 숫자가 안 읽혀 `2026년 3월부터 밀림`(연·월)으로 접음. 이미 있던 `dday()`에 먼 과거 분기를 대칭으로 추가해 표기를 한 곳에서만 만들게 함(먼 미래 90일 규칙과 같은 기준) (2) SC-05 캘린더는 밀린 항목을 원래 날짜에만 두면 지나간 달을 뒤져야 보이므로 **오늘 칸으로 끌어와** "밀림" 배지와 원래 예정일을 병기, 보이는 범위 밖 항목까지 별도 조회 (3) SC-06 리스트 카드에 "분갈이 시기" 배지 — 상세(SC-07)에 들어가야만 알 수 있던 상태를 리스트로 끌어올림. 검증 중 발견해 고친 것 2건 — 밀린 항목을 완료하면 `scheduled_date`가 그대로라 오늘 칸에서 사라지던 문제(조회 조건을 `미완료 OR 오늘 완료`로 확장), 원래 날짜 칸에서 제목과 "예정 2026-03-01"이 중복되던 문제(오늘 칸으로 끌어온 경우만 병기). 부수로 전 탭 sticky 제목에 스크롤 경계 그림자, 캘린더 점 3개 초과 시 `+n` 접기, SC-03 알림 배너를 라벤더 면 → 좌측 보더로 축소. 이번 라운드 규칙 5건을 CLAUDE.md "UX 점검 2차" 절로 신설하고, 이미 쓰고 있으면서 팔레트 표에 빠져 있던 `soil` 토큰도 함께 문서화
- **③구현 착수 순서 11번 ② — 파비콘·링크 공유 미리보기 설정.** PWA를 제외하면서 11번으로 옮겨 온 두 항목(기본 Next.js 아이콘, OG 미설정)을 처리. 로고 원본(`public/images/logo_icon.png`) 하나에서 4종을 파생 — `icon.png`(256·투명) · `favicon.ico`(64) · `apple-icon.png`(180·불투명 라임) · `opengraph-image.png`/`twitter-image.png`(1200×630). **파비콘 배경은 눈으로 비교해서 확정** — 라임·흰·ink·투명 4안을 만들어 16·24·32px로 밝은/어두운 탭 배경에 나란히 놓고 보니, 브랜드색인 라임에 올리면 초록 새싹이 배경에 묻히고 `ink`에 올리면 갈색 화분이 묻혀서 로고의 절반만 읽혔다. 두 색이 다 살아남는 건 배경 없는 원본뿐이라 **투명 풀블리드**로 결정(iOS 홈 아이콘만 예외 — 투명이 검게 깔리므로 라임 락업 유지). OG 카드는 새 디자인을 만들지 않고 SC-01 입구 화면 구성(ink 배경·라임 락업·"식물과 사는 법, 식집사가 알려줄게요")을 그대로 옮김. `layout.tsx`에 `metadataBase`를 배포 주소로 두고 `openGraph`·`twitter` 메타를 추가, 개발 서버는 Next가 localhost로 덮어쓰므로 `next build && next start`로 절대주소가 배포 주소로 나가는 것까지 확인. 트러블슈팅 — 이 맥에 ImageMagick·PIL이 없고 `sips`는 합성이 안 되며 브라우저가 `file://`을 못 열고 스크린샷도 JPEG라, 캔버스로 그려 임시 API 라우트로 저장하는 방식을 썼다(작업 후 라우트 삭제). 그 라우트를 `app/api/_devsave`로 만들었다가 404 — App Router에서 `_` 시작 폴더는 라우팅에서 제외된다

</details>

</details>

### 4단계. 배포

| 산출물 | 상태 | 비고 |
|---|---|---|
| ① 배포 관리 (체크리스트 / 릴리즈노트) | ☐ | |
| 실제 배포 | 🔄 | Vercel 자동 배포 연결 완료(main 푸시 시 배포) — https://sikjipsa-one.vercel.app · 최종 점검은 3단계 11번에서 |

**결정 로그**

<details>
<summary>2026-08-21 (1건)</summary>

- **배포 파이프라인 선연결.** 3단계 1번(프로젝트 셋업) 단계에서 GitHub → Vercel 자동 배포를 먼저 붙이고 빈 화면으로 파이프라인부터 검증했다. 이후 모든 커밋이 자동 배포되므로 4단계에서 따로 "배포 작업"을 할 필요는 없고, 남은 건 배포 관리 문서(체크리스트·릴리즈노트)뿐이다. 신규 Vercel 프로젝트에 기본으로 켜져 있던 Deployment Protection은 포트폴리오 공개용이라 해제

</details>

### 5단계. 데이터/회고

| 산출물 | 상태 | 비고 |
|---|---|---|
| ① KPI 트래킹 시트 | ☐ | |
| ② 회고 보고서 | ☐ | |
| ③ 프로젝트 요약 원페이저 | ☐ | |
| (옵션) PRD 서머리 1장 | ☐ | 필요 시 1·2단계 요약본으로 추가 |

**결정 로그**

_(아직 없음)_

## 다음 액션

### 지금 남은 작업
- [x] 구현 착수 순서 11번 ① 실사용 UX 점검·다듬기 (2026-08-23, 18건 중 17건 반영)
- [x] 11번 ① 2차 마무리 — 밀린 일정 표현·스크롤 경계 (2026-08-25)
- [x] OpenWeatherMap 키 발급·`.env.local`·Vercel 환경변수 등록 (2026-08-23, 실제 날씨 연동 확인)
- [x] 구현 착수 순서 11번 ② 파비콘·링크 공유 미리보기 설정 (2026-08-25)
- [ ] 구현 착수 순서 11번 ③ 반응형 최종 점검 → ④ 배포 마무리
- [ ] 4단계 ① 배포 관리(체크리스트 / 릴리즈노트) 작성

### 확정 필요 — 문서에 정의가 없어 임의로 구현한 것
> 전부 동작은 하지만 기획 문서에 근거가 없는 항목. 확정되면 문서와 코드 양쪽을 맞춰야 한다.

- [ ] SC-01 에러 문구 — `app/sc01/page.tsx`의 `ERROR_MESSAGES`
- [ ] 케어현황 링 3개의 의미 — 현재는 오늘 처리 대상 중 완료 비율 / 종별 권장 광량과 실제 배치 일치 비율
- [ ] 날씨 예외 동시 충족 시 우선순위 — 현재는 기온 예외(폭염→한파)를 먼저 판정
- [ ] 장마 판정 기준 — 정책은 "일 강수량 20mm 이상"이나 현재 API가 시간당 값만 줘서 20mm/24h로 환산, "3일 연속 강수"는 미구현
- [ ] 캘린더 날짜 배지의 색상 구분 기준 — 현재는 케어 유형(물주기 라임·분갈이 라벤더·완료 회색)
- [ ] 케어 리포트 지표 2종의 계산 기준 — 주간 완료율(최근 7일)·연속 관리일수
- [ ] SC-03 신규 요소 3건(식물 선택 탭 · 다가오는 일정 · 날씨 팁 문구) — 화면설계서 구성요소에 없음
- [ ] 날씨 팁 문구 7종 — `lib/weather-tips.ts`, 기획서에 없는 신규 카피(고습 분기 추가로 7종)

### 범위 밖으로 남긴 것
- [ ] 회원탈퇴 시 계정(`auth.users`) 자체 삭제 — service_role 키가 필요해 미구현, 필요하면 별도 서버 함수 검토
- [ ] 식물별 알림 on/off — 데이터정의서에 없는 필드가 필요해 보류(현재는 사용자 단위 알림만)
- [ ] 캘린더의 과거·미래 일별 날씨 아이콘 — 유료 History API가 필요해 오늘 칸만 표시

### 정리할 것
- [ ] 테스트 계정(`tester@` · `newuser@` · `uxcheck@sikjipsa.dev` + 직접 가입한 계정) — QA 끝나면 Supabase 대시보드에서 삭제
- [ ] 위치 권한 응답 대기 중 취소 수단 없음 — 대기가 길어질 때 사용자가 빠져나올 방법이 없음

### 앞 단계 잔여 (0·1단계)
- [ ] v1.0 시장성장 차트(1,216억→5,000억→1조7,000억) 원 출처 확인 — 사용자 확인 필요
- [ ] (선택) 디지털 앱 수요/초보자 실패율 관련 자료 추가 탐색, 또는 직접 미니 설문으로 보강
- [ ] 0단계 리서치 최종 검토 및 [확정]
- [ ] ⑤·⑥의 KPI 목표 수치(%)는 비워둔 상태 — 채우기
- [ ] 1단계 전체(①~⑥) 최종 검토 및 [확정]

<details>
<summary>완료 (2026-08-21 · 10건)</summary>

- [x] MVP 범위 재정의 — ⑤ MVP 로드맵에서 Phase 1(물주기)·Phase 2(분갈이·위치)로 분리해 해소
- [x] 구현 착수 순서 1번 — 프로젝트 셋업(Next.js·Supabase·GitHub·Vercel)
- [x] 구현 착수 순서 2번 — 데이터 모델 4개 테이블 + RLS
- [x] 구현 착수 순서 3번 — 정적 데이터 검수(스키마 문제 0건) + 이미지 링크 45건 전수 검증·CDN 교체·불일치 사진 5건 교체
- [x] 구현 착수 순서 4번 — SC-01 인증
- [x] 구현 착수 순서 5번 — SC-02 온보딩 · SC-08 식물 등록 · SC-06 식물 리스트
- [x] 구현 착수 순서 6번 — SC-03 · SC-04 · SC-07 + 하단 탭바
- [x] 구현 착수 순서 7번 — SC-07 분갈이 이력·배치 위치 섹션
- [x] 구현 착수 순서 8번 — SC-05 월간 캘린더 뷰
- [x] 구현 착수 순서 9~10번 — SC-11 마이페이지 · SC-12 계정 관리 · SC-09 제품 리스트 · SC-10 제품 상세 · 케어 리포트 카드

</details>
