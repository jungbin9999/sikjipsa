# CLAUDE.md — 식집사 2.0 프로젝트 규칙

이 파일은 이 프로젝트에서 작업하는 AI 코딩 도구(Claude Code 등)를 위한 규칙 문서입니다.
코드 작업 전 반드시 읽고, 여기 없는 내용이 궁금하면 아래 "기획 문서" 2개를 먼저 참고하세요.

## 프로젝트 개요

- 식집사 2.0 — 날씨를 아는 AI 식물집사, 초보 식집사를 위한 물주기·분갈이·화분위치 케어 앱
- **포트폴리오용 MVP 프로토타입입니다. 실제 서비스 출시가 아닙니다.** 비용이 들거나 과도하게 복잡한 인프라는 절대 만들지 마세요.
- 기획 문서(단일 소스, 코드보다 우선):
  - `식집사 업그레이드 프로젝트.md` — 서비스 전략·화면설계·데이터모델·정책 전체 내용
  - `spec.md` — 진행 상태·결정 로그
  - 화면/데이터/정책 관련해서 애매하면 임의로 판단하지 말고 이 두 파일을 먼저 찾아보고, 그래도 없으면 사용자에게 질문할 것
  - 프로젝트 전체를 빨리 파악해야 하면 기획서 **5단계 ③ 프로젝트 요약 원페이저**부터 볼 것(한 장 요약 + 알려진 한계)

## 기술 스택

| 영역 | 선택 |
|---|---|
| 프론트엔드 | Next.js (React) |
| 스타일링 | Tailwind CSS |
| 백엔드 · DB · 인증 | Supabase (PostgreSQL + Auth, 이메일 로그인만) |
| 날씨 데이터 | OpenWeatherMap 무료 티어 |
| 식물 종류 · 제품 카탈로그 | 코드 내 JSON (DB 테이블 아님) |
| 배포 | Vercel |
| PWA | **제외** — 설치·오프라인이 포트폴리오 열람자에게 가치가 없고 `next-pwa`도 방치된 패키지라 3단계에서 제외 결정. manifest·서비스워커를 만들지 말 것 |
| 상태 관리 | 별도 라이브러리 없이 React 기본 state |

## 폴더 구조 (제안)

```
/app                  # Next.js 라우트, 화면ID(SC-01~SC-12)를 그대로 사용
  /api/weather        # OpenWeatherMap 호출(키를 클라이언트에 노출하지 않기 위해 서버 경유)
  /api/devseed        # 데모 계정 데이터 시드 — 개발 환경 전용(배포본 404). 지우지 말 것, 재시드에 필요
  favicon.ico         # 탭 파비콘(64px) — 아래 icon.png에서 생성, 손으로 고치지 말 것
  icon.png            # 탭 파비콘(256px, 투명 풀블리드). 파일명이 곧 규약이라 이름 바꾸지 말 것
  apple-icon.png      # iOS 홈 화면(180px, 불투명 라임). 투명은 검게 깔려서 배경을 채운다
  opengraph-image.png # 링크 공유 미리보기(1200×630) — twitter-image.png와 같은 이미지
/components
  PhoneFrame.tsx      # 데스크톱에서 430x860 폰 형태로 감싸는 래퍼(layout.tsx에 적용)
  TabBar.tsx          # 하단 탭바 5개
  CareRing.tsx        # 케어현황 링
  CareStatCard.tsx    # 라벨/큰 값/보조 설명 3단 카드
  PlantSelector.tsx   # 오늘 탭 식물 선택 가로 칩
  TabIcons.tsx        # 하단 탭바 선 아이콘 5종
  SpeciesCombobox.tsx # 식물 종류 검색·선택
/lib
  weather.ts          # 날씨 조회(3초 타임아웃) + 시연용 WEATHER_SCENARIOS
  weather-tips.ts     # 날씨 상태별 관리 팁 문구(기획서에 없는 신규 카피 — 여기만 교체)
  supabase.ts         # Supabase 클라이언트
  care-calc.ts        # 물주기·분갈이 예정일 계산(순수 함수)
  care-service.ts     # on-demand 재계산 · 오늘/다가오는 할일 조회 · 완료 처리 · 배치·화분 변경
  care-report.ts      # 주간 완료율·연속 관리일수 집계(저장 엔티티 아님)
  calendar.ts         # 캘린더 날짜 격자·범위 이동·라벨
  location.ts         # 위치 권한 요청과 서울 기준값 폴백(SC-02·SC-11 공용)
  plants.ts           # plants.json 접근 헬퍼
  products.ts         # products.json 접근 헬퍼 + 분갈이 연동 추천
  tone.ts             # 리스트 카드 색 로테이션(흰·블랙·라임)
  demo.ts             # 포트폴리오 데모 계정 자격증명 + signInAsDemo(). 공개가 전제인 계정이라 코드에 그대로 두는 게 의도된 설계
/data
  plants.json         # 식물 종류 29종(하드코딩, 이미 작성 완료 — 별도 전달된 파일 그대로 사용, 임의로 종류·필드 추가/변경하지 말 것)
  products.json        # 제품 카탈로그 목업 16개(작성 완료 — 임의로 항목·필드 추가/변경하지 말 것)
/types                # 데이터정의서 6개 엔티티 기준 타입 정의
/supabase/migrations  # DB 스키마(변경은 새 마이그레이션 파일로, 기존 파일 수정 금지)
/design-refs          # 디자인 참고 이미지·색 팔레트
/docs/dev-log.md      # 데일리 개발로그
```

## 데이터 모델

- 데이터정의서(식집사 업그레이드 프로젝트.md → 2단계 ③) 기준, 엔티티 6개: 사용자 · 식물 · 케어 이력 · 날씨 데이터 · 알림 · 제품
- **실제 DB 테이블은 4개** — `profiles` · `plants` · `care_logs` · `notifications`. 제품은 `/data/products.json` 목업, 날씨는 조회성이라 저장하지 않는다(둘 다 테이블 없음)
- **`notifications`는 스키마만 있고 쓰는 코드가 없다** — 실제 발송이 MVP 범위 밖이라 이력이 쌓이지 않는다. 앱이 실제로 접근하는 테이블은 `plants` · `care_logs` · `profiles` 3개뿐. 비어 있는 걸 버그로 오해하지 말 것
- 스키마 변경은 `supabase/migrations/`에 **새 마이그레이션 파일을 추가**할 것 — 이미 적용된 파일을 수정하지 말 것
- 필드명은 데이터정의서에 정의된 영문 snake_case를 그대로 사용(예: `next_watering_date`, `is_completed`, `plant_id`) — 임의로 새 필드명을 만들지 말 것
- PK/FK 관계도 데이터정의서 표기 그대로(예: 식물의 `user_id`는 사용자를 참조하는 FK)
- 사용자 저장 방식 — 인증(이메일·비밀번호)은 Supabase Auth(`auth.users`)가 처리, `location` / `notification_permission` / `nickname` / `profile_image_url`처럼 `auth.users`에 없는 앱 전용 필드는 `auth.users`와 1:1 연결된 별도 `profiles` 테이블에 저장(`auth.users`를 직접 확장하지 말 것)
- `care-calc.ts`(물주기·분갈이 계산)는 데이터정의서 "핵심 계산 로직" 섹션의 공식을 그대로 구현할 것 — 임의로 다른 계산식을 만들지 말 것(물주기: 종별 기본 간격 × 계절 배율(봄1.0·여름0.8·가을1.0·겨울1.6) + 날씨 보정(폭염 -1일 · 한파 +2일 · 장마 +2일), 분갈이: 생장속도별 기본 개월 + 화분크기 보정 + 계절 보정)

## 화면

- SC-01~SC-12, 화면설계서(2단계 ②) 기준 — 화면ID를 라우트·컴포넌트명에 그대로 사용(예: `app/sc07/page.tsx`, `PlantDetailScreen`)
- 화면 12개 모두 구현 완료(2026-08-21). 진입점 `/`는 세션 유무로 SC-01·SC-03 분기
- 구현 착수 순서 12단계 완료 + **0~5단계 전 산출물 작성 종료**(2026-08-26) — 프로덕션 https://sikjipsa-one.vercel.app 에서 동작 확인. 남은 건 lint 정리·테스트 계정 정리 같은 뒷정리뿐
- 데모 계정으로 가입 없이 둘러볼 수 있음(`lib/demo.ts`) — 시드가 오늘 기준 상대 날짜라 **포트폴리오를 보여주기 전에 재시드**할 것(`/api/devseed`, 개발 환경 전용). 절차는 기획서 4단계 ① 배포 체크리스트 참조
- 로그아웃은 SC-12 소관 — 다른 화면에 중복해서 두지 말 것
- 화면 흐름(push/pop/replace/tab전환)은 화면설계서의 "공통 내비게이션 규칙"을 그대로 따를 것

## 디자인

- 참고 이미지 — `/design-refs` (파일명이 참고 용도, `typo_*` 타이포 · `color_*` 색 · `design_*` 전체 UI)
- 색 팔레트는 `design-refs/color_03_palette.png`가 단일 소스, 토큰은 `app/globals.css`의 `@theme`에 정의 — 여기 없는 색을 새로 만들지 말 것

| 토큰 | 값 | 용도 |
|---|---|---|
| `accent` | `#C9F075` | 라임. "지금 해야 할 것"(케어 완료 버튼·오늘 할일·진행률)에만 |
| `ink` | `#08080A` | 텍스트 기본색 겸 다크 화면 배경 |
| `paper` | `#FFFFFF` | 카드·입력 필드 등 표면 |
| `cloud` | `#F6F6F6` | 라이트 화면 기본 배경 |
| `lilac` | `#CCBCF6` | 아주 작은 포인트 요소로만. 분갈이 계열 표시(캘린더 배지·"분갈이 시기"·"밀림")를 여기로 통일 |
| `soil` | `#5B3A29` | 로고 화분의 브라운. 흙·화분 관련 요소(화분 크기 칩)에만 소량 |
| `danger` / `danger-soft` | `#D92D20` / `#FF9B96` | 팔레트 밖 기능색, 에러 표시 전용(라이트/다크) |

- 배경 톤 — 라이트가 기본, **입구 화면(SC-01 로그인 · SC-02 온보딩)만 다크(`ink`)**
- `accent` 위 텍스트는 항상 `ink` — 흰 텍스트는 대비 미달
- **입력 필드는 다크 화면에서도 `bg-paper`(흰 표면) + `text-ink`** — 다크 배경에 흰 글자로 두면 비밀번호 관리자 자동완성이 자기 색(밝은 배경 + 어두운 글자)으로 덮어써서 글자가 안 보인다. CSS로 이기려는 시도(전환 지연 · inset 그림자 · `!important`)는 키체인에서 통하지 않았고, 브라우저 자동완성 스타일이 전부 밝은 표면을 전제하므로 애초에 밝게 두는 쪽이 맞다(`paper` 토큰의 원래 정의도 "카드·입력 필드 표면")
- 보조 텍스트는 새 회색을 만들지 말고 `text-ink/60`처럼 투명도로 처리
- 형태 — 카드 라운드 `rounded-card`(24px), 입력 필드 `rounded-field`(16px), 버튼·세그먼트 토글은 pill(`rounded-full`)
- 타이포 — 헤드라인은 굵게(`font-extrabold`), 라벨·보조 텍스트는 작고 얇게
- 기준 뷰포트는 **430×860**(`components/PhoneFrame.tsx`) — 모든 화면은 이 크기 기준으로 작성. 데스크톱에서는 창 크기에 맞춰 프레임 전체가 통째로 축소되므로 화면마다 별도 대응 불필요
- 프레임에 transform이 걸려 있어 그 안의 `position: fixed`는 브라우저 창이 아니라 프레임 기준으로 잡힘(하단 탭바 등에 그대로 사용 가능)
- 동작 모드는 딱 둘 — **462px(430+여백 32) 미만이면 프레임 없이 전체 화면, 그 이상이면 프레임**. 미디어쿼리(`sm:`·`md:`)는 쓰지 않는다
  - 즉 실기기 대응이 필요한 폭은 **320~430px 한 구간뿐**. 그 위는 전부 프레임이 처리한다
  - 하한은 **320px**(가장 좁은 실기기) — 새 UI는 이 폭에서 확인할 것
  - 좁은 폭에서 칩·배지가 여러 개 붙는 줄은 `flex-wrap` + 각 칩에 `shrink-0`·`whitespace-nowrap`을 줄 것. 안 주면 flex가 칩을 눌러 **한 글자씩 세로로 쪼개진다**
  - 가로로 넘칠 수밖에 없는 줄(카테고리 칩·추천 목록)은 `overflow-x-auto` + `shrink-0`으로 스크롤시킬 것. 줄바꿈하지 말 것

### UX 점검(2026-08-23)에서 확정된 규칙

- **텍스트 대비** — 보조 텍스트는 `/60`을 하한으로 쓸 것. `/40`·`/50`은 WCAG AA(4.5:1) 미달이라 쓰지 말 것
- **터치 타겟** — 누를 수 있는 요소는 최소 44px. 시각 크기는 유지하고 패딩으로 확보
- **아이콘** — 이모지와 텍스트 기호를 섞지 말 것. 탭바는 `TabIcons.tsx`의 선 아이콘으로 통일
- **상태 표현** — 색 하나에만 의존하지 말 것. 오늘의 할일은 카드색(블랙↔흰)·원색(연두↔그레이)·버튼 글자(완료↔취소)를 함께 뒤집어 구분한다. 취소선은 쓰지 않고 불투명도로 처리
- **버튼 안 라벨** — 글자와 아이콘을 섞지 말고 한 종류로 통일. 글자를 쓸 땐 "누르면 무엇이 되는지"를 그대로 쓴다
- **화면 제목** — 각 탭 제목은 `sticky top-0`으로 고정해 스크롤해도 어느 화면인지 보이게 한다
- **먼 날짜** — 90일이 넘는 일정은 `D-921` 같은 세 자리 D-day 대신 "2029년 3월"처럼 연·월로 표기
- **날씨 카드** — 색으로 면을 가르지 말 것(블랙/라임 반씩 나누면 별개 정보처럼 보임). 라임은 세로 바·라벨·일정 반영 문구 같은 강조에만 사용
- **케어현황 링** — 세 링 모두 "전체 식물 중 지금 문제없는 식물 수"가 기준. 분모를 다르게 쓰지 말 것

### UX 점검 2차(2026-08-25)에서 확정된 규칙

- **밀린 일정** — 지난 일정은 "예정일이 언제였나"가 아니라 **"얼마나 밀렸나"**로 보여준다. 날짜 문자열(`2026-03-01`)을 그대로 두지 말 것
  - 표기는 `dday()` 한 곳(`app/sc03/page.tsx`)에서만 만든다 — 오늘 / `D-n` / `n일 지남` / 90일 넘게 밀리면 `2026년 3월부터 밀림`
  - 밀린 항목은 캘린더에서 원래 날짜에 두지 말고 **오늘 칸으로 함께 끌어와** "밀림" 배지와 원래 예정일을 병기(SC-05)
  - 완료해도 오늘 칸에서 사라지지 않게, 밀린 항목 조회는 `scheduled_date < 오늘 AND (미완료 OR 오늘 완료)`로 가져온다
- **스크롤 경계** — `sticky` 제목 아래에 얇은 그림자를 깔아 콘텐츠가 제목 밑으로 지나가는 것을 보이게 한다
- **다음 행동이 있는 상태는 리스트에서도 보인다** — 분갈이 시기가 된 식물은 상세(SC-07)에 들어가야만 알 수 있게 두지 말고 리스트 카드에도 배지로 노출(SC-06)
- **점·배지가 넘칠 때** — 캘린더 날짜 칸은 점 3개까지만 찍고 나머지는 `+n`으로 접는다. 칸을 늘리지 말 것
- **색이 상태를 뜻하면 라벨도 같이 바꿀 것** — SC-06 "다음" 블록은 `다음`(미래·블랙) / `오늘`(라임) / `지남`(라임)으로 라벨이 갈린다. 색만 바꾸고 라벨을 `다음`으로 두면 라임과 블랙의 차이를 읽을 근거가 화면에 없다
- **목록 정렬은 급한 순** — 내 식물(SC-06)은 `next_watering_date` 오름차순. 등록순으로 두면 지연된 식물이 여유 있는 식물 아래로 묻힌다
- **행 안의 상태 배지는 오른쪽 끝으로** — 이름 뒤에 바로 붙이면 이름 길이에 따라 위치가 들쭉날쭉해진다(SC-05 "밀림")
- **터치 타겟과 세로 정렬은 같이 준다** — `min-h-11`만 주면 글자가 박스 위쪽에 붙어 아래 줄과 벌어진다. `flex items-center`를 함께 쓸 것
- **지킬 수 없는 약속을 문구로 쓰지 말 것** — 되돌릴 화면이 없으면 "나중에 다시 꺼낼 수 있어요" 같은 표현을 쓰지 않는다(SC-07 보관 문구가 그랬다)

### 파비콘·링크 미리보기

- 브랜드 마크는 **🌱 이모지**로 통일(2026-08-25) — SC-01 입구 화면 · 파비콘 3종 · OG 카드가 모두 같은 얼굴. `public/images/logo_icon.png`는 더 이상 아이콘 파생에 쓰지 않는다
- **탭 파비콘은 투명 풀블리드** — 라임 배경에 올리면 초록 새싹이 묻힌다(16·24·32px로 비교해 확정). 불투명이 필수인 iOS 홈 아이콘(`apple-icon.png`)만 예외로 **`ink` 배경** — 라임은 여기서도 묻히고, 초록 새싹은 검정 위에서 가장 또렷하다
- OG 카드는 **좌우 2단** — 왼쪽은 🌱 + "식집사" 워드마크 · 3줄 헤드라인 · 기능 칩 3개, 오른쪽은 실제 앱 카드 미리보기 3장. 배경은 `ink`(다크), 오른쪽에 라임 방사형 그라디언트를 아주 옅게
  - 오른쪽 카드는 앱 UI와 같은 규칙을 지킬 것 — 물주기 라임 · 분갈이 라벤더 배지, "오늘"만 블랙+라임 강조
  - 카드 안 여백은 위아래를 같게(22px) — 배지와 이름줄 사이만 벌리고 아래를 붙이면 글자가 바닥에 눌린 것처럼 보인다
  - 그라디언트는 캔버스 전체에 깔 것. 일부 영역만 칠하면 경계에 세로 이음매가 남는다
- `metadataBase`(`app/layout.tsx`)는 배포 주소 `https://sikjipsa-one.vercel.app` 기준. 개발 중엔 Next가 localhost로 덮어쓰므로 미리보기 주소 확인은 `next build && next start`로 할 것

## MVP 구현 범위 — 반드시 지킬 것

아래는 전부 "MVP 구현 범위" 표(3단계 상단)와 1:1 대응. 임의로 범위를 넓히지 말 것.

- 실제 디바이스 푸시 발송(FCM/APNs) 구현 금지 — 알림 설정 on/off UX만 구현(SC-11)
- 서버 스케줄러(cron) 금지 — 물주기·분갈이 예정일은 화면 진입 시 on-demand로만 재계산
- 식물 종류 검색은 `/data/plants.json` 안에서만 — 외부 식물 DB API 연동 금지, 29종 규모이므로 서버 검색 API를 따로 만들지 말고 클라이언트 사이드에서 타이핑에 맞춰 필터링하는 콤보박스(자동완성 select)로 구현
- 제품 카탈로그는 `/data/products.json` 목업 — 실제 제휴사 API 연동 금지
- 로그인은 이메일만 — 소셜 로그인 붙이지 말 것
- 이미지 업로드 기능 만들지 말 것 — 식물 사진은 종류별 기본 이미지(`plants.json`에 이미지 URL 포함), 프로필 사진은 기본 아바타 고정
- 정책정의서(2단계 ④)의 세부 수치(재알림 횟수·발송시간대·데이터 보존기간 등 12건)는 실제로 강제하는 로직 구현하지 말 것 — 문서 참고용으로만 존재
- 위치·알림 권한 거부 시 폴백(서울 기준값 · 인앱 배지)과 날씨 임계값 기반 재계산처럼 Must-have 핵심 기능에 해당하는 로직은 구현 대상(정책정의서 예외처리 규칙 표 참조), 세부 수치만 단순화

## 코딩 컨벤션

- TypeScript 사용
- 컴포넌트는 PascalCase, 함수·변수는 camelCase
- 커밋 메시지는 화면ID 또는 기능 단위로 짧게(예: `feat: SC-08 식물 등록 폼 구현`)
- 기획 문서에 없는 기능·필드·화면을 임의로 추가하지 말 것 — 필요하면 먼저 사용자에게 확인

## 테스트 · 에러 처리 · 환경변수

- 자동화된 테스트 코드는 작성하지 않음 — ⑤QA 체크리스트로 수동 검증, 요청 없이 테스트 슈트를 만들지 말 것
- `npx eslint app components`는 **에러 0건이 정상** — 깨진 채로 두지 말 것
  - `react-hooks/set-state-in-effect` 규칙은 **켜 둔다.** 진입 시 데이터 로드 4곳(sc03·sc05·sc07·sc11)만 사유를 적어 개별 예외 처리했고, 규칙을 통째로 끄면 파생 상태를 effect로 보정하는 진짜 문제를 놓친다(실제로 SC-05 선택 날짜 보정이 이 규칙에 잡혔다)
  - **다른 state에서 계산되는 값은 state로 저장하지 말고 렌더 중에 구할 것** — effect로 되돌리면 렌더가 한 번 더 돌면서 엉뚱한 화면이 한 번 스친다(SC-05 `selectedDate`가 그 예)
- 날씨 경보 임계값(폭염 33 · 한파 -12)은 정책정의서 수치를 그대로 쓰되, **비교는 화면 표시와 같은 정수 단위로** 한다(`app/api/weather/route.ts`). 원값으로 비교하면 32.76도가 화면엔 `33°`로 뜨면서 폭염 판정은 빠져 서로 어긋난다
  - 무료 현재날씨 API의 `temp_max`·`temp_min`은 지점 현재값과 사실상 같아, 실제로는 "일 최고·최저기온"이 아니라 현재기온으로 판정하는 셈 — 알고 있는 한계이므로 다른 계산식으로 바꾸지 말 것
- 날씨 예외(폭염·한파·장마)는 실제 날씨로 재현할 수 없으므로 시연·QA용 강제 스위치를 쓴다 — `/sc03?weather=장마` (`맑음`·`폭염`·`한파`·`장마`). 이때 API를 호출하지 않고 `lib/weather.ts`의 `WEATHER_SCENARIOS` 값을 사용하며, 배너에 "시연" 배지가 붙는다. 파라미터가 없으면 항상 실제 API를 호출한다
- API 호출(날씨·Supabase) 실패 시엔 화면별 상세정의의 "상태 분기"에 정의된 에러 문구·폴백을 그대로 사용, 정의가 없는 화면이면 임의로 만들지 말고 먼저 질문할 것
- Supabase·OpenWeatherMap 키 등 환경변수는 `.env.local`에 저장하고 `.gitignore`에 포함, 코드에 직접 하드코딩하지 말 것

## 계측·지표

- **분석/계측 패키지를 임의로 설치하지 말 것** — 현재 의존성은 `next` · `react` · `react-dom` · `@supabase/supabase-js` 4개뿐이고, 이벤트 로그 도입은 5단계 ① KPI 트래킹 시트에서 **"실서비스로 간다면 할 일"**로 분류돼 있다(지금 범위 아님). 필요하면 먼저 사용자에게 확인
- 지표를 다뤄야 할 땐 기획서 **5단계 ① KPI 트래킹 시트**를 먼저 볼 것 — 1단계 ⑥ 지표 5종 중 무엇이 지금 측정 가능하고 무엇이 왜 막혀 있는지 정리돼 있다
- **KPI 시트에 수치를 채우지 말 것** — 실사용자가 없어 숫자를 넣으면 지어낸 값이 된다(1단계 ⑥에서 목표 수치를 비워둔 것과 같은 이유)
- 사용자 개인 단위 지표는 `lib/care-report.ts`가 이미 계산한다(주간 완료율·연속 관리일수) — 전체 집계가 필요해도 새 로직을 만들기 전에 이 파일을 먼저 볼 것

## 하지 말아야 할 것

- 유료 API·서비스 사용 금지(전부 무료 티어 내에서 해결)
- 정책정의서에 없는 새로운 예외처리 규칙 임의 추가 금지
- 데이터정의서에 없는 필드 임의 추가 금지
- 화면설계서에 없는 화면·네비게이션 경로 임의 추가 금지
- 팔레트(`app/globals.css`의 `@theme`)에 없는 색 임의 추가 금지 — 에러용 `danger`·`danger-soft`만 예외로 이미 정의돼 있음
- `next dev`가 이 파일에 자체 블록을 덧붙이지 않도록 `next.config.ts`에 `agentRules: false`가 걸려 있음 — 지우지 말 것