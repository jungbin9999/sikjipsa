# CLAUDE.md — 식집사 2.0 프로젝트 규칙

이 파일은 이 프로젝트에서 작업하는 AI 코딩 도구(Claude Code 등)를 위한 규칙 문서입니다.
코드 작업 전 반드시 읽고, 여기 없는 내용이 궁금하면 아래 "기획 문서" 2개를 먼저 참고하세요.

## 프로젝트 개요

- 식집사 2.0 — 날씨를 아는 AI 식물집사, 초보 식집사를 위한 물주기·분갈이·화분위치 케어 앱
- **포트폴리오용 MVP 프로토타입입니다. 실제 서비스 출시가 아닙니다.** 비용이 들거나 과도하게 복잡한 인프라는 절대 만들지 마세요.
- 기획 문서(단일 소스, 코드보다 우선):
  - `식집사 업그레이드 프로젝트.md` — 서비스 전략·화면설계·데이터모델·정책 전체 내용
  - `spec.md` — 진행 상태·결정 로그
  - 화면/데이터/정책 관련해서 애매하면 임의로 판단하지 말고 이 두 파일을 먼저 찾아보고, 그래도 없으면 사용자에게 질문할 것

## 기술 스택

| 영역 | 선택 |
|---|---|
| 프론트엔드 | Next.js (React) |
| 스타일링 | Tailwind CSS |
| 백엔드 · DB · 인증 | Supabase (PostgreSQL + Auth, 이메일 로그인만) |
| 날씨 데이터 | OpenWeatherMap 무료 티어 |
| 식물 종류 · 제품 카탈로그 | 코드 내 JSON (DB 테이블 아님) |
| 배포 | Vercel |
| PWA | next-pwa 최소 설정(manifest + 서비스워커) |
| 상태 관리 | 별도 라이브러리 없이 React 기본 state |

## 폴더 구조 (제안)

```
/app                  # Next.js 라우트, 화면ID(SC-01~SC-12)를 그대로 사용
  /api/weather        # OpenWeatherMap 호출(키를 클라이언트에 노출하지 않기 위해 서버 경유)
/components
  PhoneFrame.tsx      # 데스크톱에서 430x860 폰 형태로 감싸는 래퍼(layout.tsx에 적용)
  TabBar.tsx          # 하단 탭바 5개
  CareRing.tsx        # 케어현황 링
  SpeciesCombobox.tsx # 식물 종류 검색·선택
/lib
  weather.ts          # 날씨 조회(3초 타임아웃) + 시연용 WEATHER_SCENARIOS
  supabase.ts         # Supabase 클라이언트
  care-calc.ts        # 물주기·분갈이 예정일 계산(순수 함수)
  care-service.ts     # on-demand 재계산 · 오늘 할일 조회 · 완료 처리
  plants.ts           # plants.json 접근 헬퍼
/data
  plants.json         # 식물 종류 29종(하드코딩, 이미 작성 완료 — 별도 전달된 파일 그대로 사용, 임의로 종류·필드 추가/변경하지 말 것)
  products.json        # 제품 카탈로그 목업 16개(작성 완료 — 임의로 항목·필드 추가/변경하지 말 것)
/types                # 데이터정의서 6개 엔티티 기준 타입 정의
/supabase/migrations  # DB 스키마(변경은 새 마이그레이션 파일로, 기존 파일 수정 금지)
/design-refs          # 디자인 참고 이미지·색 팔레트
/docs/dev-log.md      # 데일리 개발로그
```

## 데이터 모델

- 데이터정의서(식집사 업그레이드 프로젝트.md → 2단계 ③) 기준, 엔티티 6개: 사용자 · 식물 · 케어 이력 · 날씨 데이터 · 알림 · 제품
- 필드명은 데이터정의서에 정의된 영문 snake_case를 그대로 사용(예: `next_watering_date`, `is_completed`, `plant_id`) — 임의로 새 필드명을 만들지 말 것
- PK/FK 관계도 데이터정의서 표기 그대로(예: 식물의 `user_id`는 사용자를 참조하는 FK)
- 사용자 저장 방식 — 인증(이메일·비밀번호)은 Supabase Auth(`auth.users`)가 처리, `location` / `notification_permission` / `nickname` / `profile_image_url`처럼 `auth.users`에 없는 앱 전용 필드는 `auth.users`와 1:1 연결된 별도 `profiles` 테이블에 저장(`auth.users`를 직접 확장하지 말 것)
- `care-calc.ts`(물주기·분갈이 계산)는 데이터정의서 "핵심 계산 로직" 섹션의 공식을 그대로 구현할 것 — 임의로 다른 계산식을 만들지 말 것(물주기: 종별 기본 간격 × 계절 배율(봄1.0·여름0.8·가을1.0·겨울1.6) + 날씨 보정(폭염 -1일 · 한파 +2일 · 장마 +2일), 분갈이: 생장속도별 기본 개월 + 화분크기 보정 + 계절 보정)

## 화면

- SC-01~SC-12, 화면설계서(2단계 ②) 기준 — 화면ID를 라우트·컴포넌트명에 그대로 사용(예: `app/sc07/page.tsx`, `PlantDetailScreen`)
- 화면 흐름(push/pop/replace/tab전환)은 화면설계서의 "공통 내비게이션 규칙"을 그대로 따를 것

## 디자인

- 참고 이미지 — `/design-refs` (파일명이 참고 용도, `typo_*` 타이포 · `color_*` 색 · `design_*` 전체 UI)
- 색 팔레트는 `design-refs/color_03_palette.png`가 단일 소스, 토큰은 `app/globals.css`의 `@theme`에 정의 — 여기 없는 색을 새로 만들지 말 것

| 토큰 | 값 | 용도 |
|---|---|---|
| `accent` | `#C9F075` | 라임. "지금 해야 할 것"(케어 완료 버튼·오늘 할일·진행률)에만 |
| `ink` | `#08080A` | 텍스트 기본색 겸 다크 화면 배경 |
| `paper` | `#FFFFFF` | 카드·입력 필드 등 표면 |
| `cloud` | `#F6F6F6` | 라이트 화면 기본 배경 |
| `lilac` | `#CCBCF6` | 아주 작은 포인트 요소로만 |
| `danger` / `danger-soft` | `#D92D20` / `#FF9B96` | 팔레트 밖 기능색, 에러 표시 전용(라이트/다크) |

- 배경 톤 — 라이트가 기본, **입구 화면(SC-01 로그인 · SC-02 온보딩)만 다크(`ink`)**
- `accent` 위 텍스트는 항상 `ink` — 흰 텍스트는 대비 미달
- 보조 텍스트는 새 회색을 만들지 말고 `text-ink/60`처럼 투명도로 처리
- 형태 — 카드 라운드 `rounded-card`(24px), 입력 필드 `rounded-field`(16px), 버튼·세그먼트 토글은 pill(`rounded-full`)
- 타이포 — 헤드라인은 굵게(`font-extrabold`), 라벨·보조 텍스트는 작고 얇게
- 기준 뷰포트는 **430×860**(`components/PhoneFrame.tsx`) — 모든 화면은 이 크기 기준으로 작성. 데스크톱에서는 창 크기에 맞춰 프레임 전체가 통째로 축소되므로 화면마다 별도 대응 불필요
- 프레임에 transform이 걸려 있어 그 안의 `position: fixed`는 브라우저 창이 아니라 프레임 기준으로 잡힘(하단 탭바 등에 그대로 사용 가능)

## MVP 구현 범위 — 반드시 지킬 것

아래는 전부 "MVP 구현 범위" 표(3단계 상단)와 1:1 대응. 임의로 범위를 넓히지 말 것.

- 실제 디바이스 푸시 발송(FCM/APNs) 구현 금지 — 알림 설정 on/off UX만 구현(SC-11)
- 서버 스케줄러(cron) 금지 — 물주기·분갈이 예정일은 화면 진입 시 on-demand로만 재계산
- 식물 종류 검색은 `/data/plants.json` 안에서만 — 외부 식물 DB API 연동 금지, 29종 규모이므로 서버 검색 API를 따로 만들지 말고 클라이언트 사이드에서 타이핑에 맞춰 필터링하는 콤보박스(자동완성 select)로 구현
- 제품 카탈로그는 `/data/products.json` 목업 — 실제 제휴사 API 연동 금지
- 로그인은 이메일만 — 소셜 로그인 붙이지 말 것
- 이미지 업로드 기능 만들지 말 것 — 식물 사진은 종류별 기본 이미지(`plants.json`에 이미지 URL 포함), 프로필 사진은 기본 아바타 고정
- 정책정의서(2단계 ④)의 세부 수치(재알림 횟수·발송시간대·데이터 보존기간 등 12건)는 실제로 강제하는 로직 구현하지 말 것 — 문서 참고용으로만 존재
- 위치·알림 권한 거부 시 폴백(서울 기준값 · 인앱 배지)과 날씨 임계값 기반 재계산처럼 Must-have 핵심 기능에 해당하는 로직은 구현 대상(정책정의서 예외처리 규칙 표 참조), 세부 수치만 단순화

## 코딩 컨벤션

- TypeScript 사용
- 컴포넌트는 PascalCase, 함수·변수는 camelCase
- 커밋 메시지는 화면ID 또는 기능 단위로 짧게(예: `feat: SC-08 식물 등록 폼 구현`)
- 기획 문서에 없는 기능·필드·화면을 임의로 추가하지 말 것 — 필요하면 먼저 사용자에게 확인

## 테스트 · 에러 처리 · 환경변수

- 자동화된 테스트 코드는 작성하지 않음 — ⑤QA 체크리스트로 수동 검증, 요청 없이 테스트 슈트를 만들지 말 것
- 날씨 예외(폭염·한파·장마)는 실제 날씨로 재현할 수 없으므로 시연·QA용 강제 스위치를 쓴다 — `/sc03?weather=장마` (`맑음`·`폭염`·`한파`·`장마`). 이때 API를 호출하지 않고 `lib/weather.ts`의 `WEATHER_SCENARIOS` 값을 사용하며, 배너에 "시연" 배지가 붙는다. 파라미터가 없으면 항상 실제 API를 호출한다
- API 호출(날씨·Supabase) 실패 시엔 화면별 상세정의의 "상태 분기"에 정의된 에러 문구·폴백을 그대로 사용, 정의가 없는 화면이면 임의로 만들지 말고 먼저 질문할 것
- Supabase·OpenWeatherMap 키 등 환경변수는 `.env.local`에 저장하고 `.gitignore`에 포함, 코드에 직접 하드코딩하지 말 것

## 하지 말아야 할 것

- 유료 API·서비스 사용 금지(전부 무료 티어 내에서 해결)
- 정책정의서에 없는 새로운 예외처리 규칙 임의 추가 금지
- 데이터정의서에 없는 필드 임의 추가 금지
- 화면설계서에 없는 화면·네비게이션 경로 임의 추가 금지